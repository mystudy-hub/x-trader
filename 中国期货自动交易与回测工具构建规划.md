# 中国期货自动交易与回测工具构建规划（可靠性修订版）

> QH-Trader 面向中国期货，采用 Port/Adapter 架构，使回测与实盘共享订单状态机、持仓、资金账本、交易规则和事前风控。
> 首先交付可逐笔对账的单账户 Bar 回测与向量化快速研究通道，再扩展多品种移仓、模拟盘、看门狗、影子模式和最小手数实盘，最后按策略类别启用 Tick 仿真。连续合约用于信号计算，实际合约承担成交与记账；生产能力以规则核验、故障恢复、结算单对账和真实执行偏差为依据。
> 本文定义待实现的工程方案。交易所、期货公司、CTP 版本及数据供应商的具体规则，应保存来源、生效时间和验证用例；未核实的能力不得作为实盘默认值。
> 修订日期：2026-09-13，已整合第二轮深度架构评审的复核意见。

---

## 目录

- [一、核心认知与设计边界](#chapter-1)
  - [1.1 目标策略类别与分层要求](#section-1-1)
  - [1.2 修订依据](#section-1-2)
- [二、前期准备清单](#chapter-2)
- [三、参考架构与共享交易内核](#chapter-3)
  - [3.1 参考架构背景](#section-3-1)
  - [3.2 保留的设计](#section-3-2)
  - [3.3 中国期货场景的适配](#section-3-3)
  - [3.4 Port/Adapter 与共享领域逻辑](#section-3-4)
- [四、核心业务逻辑与可靠性方案](#chapter-4)
  - [4.1 平仓路由、订单状态与持仓冻结](#section-4-1)
  - [4.2 交易日历与结算事件](#section-4-2)
  - [4.3 主力映射、连续合约与真实移仓](#section-4-3)
  - [4.4 资金账本、保证金与追保](#section-4-4)
  - [4.5 事前风控与熔断执行](#section-4-5)
  - [4.6 断线恢复、事件去重与对账](#section-4-6)
- [五、数据存储与实验版本管理](#chapter-5)
  - [5.1 行情分段录制与可靠发布](#section-5-1)
  - [5.2 Bar、Tick 与结算数据 Schema](#section-5-2)
  - [5.3 DuckDB 分析边界](#section-5-3)
  - [5.4 SQLite、历史规则与费用模型](#section-5-4)
  - [5.5 完整回测实验快照](#section-5-5)
- [六、撮合模型、延迟与策略验证](#chapter-6)
  - [6.1 Bar 撮合与信息可见性](#section-6-1)
  - [6.2 Tick 撮合、涨跌停与排队估算](#section-6-2)
  - [6.3 基于时间的报单与撤单延迟](#section-6-3)
  - [6.4 样本外验证与敏感性分析](#section-6-4)
  - [6.5 影子模式与实际执行校准](#section-6-5)
  - [6.6 历史行情驱动的全链路回放](#section-6-6)
- [七、工程目录规划](#chapter-7)
- [八、运维、数据质量与可观测性](#chapter-8)
- [九、实施路线与验收标准](#chapter-9)
  - [9.1 分步实施路线](#section-9-1)
  - [9.2 必须通过的验收案例](#section-9-2)
    - [A25 交易时段与集合竞价边界子用例](#acceptance-a25)
  - [9.3 交付与上线条件](#section-9-3)

---

<a id="chapter-1"></a>

## 一、核心认知与设计边界

| 规则维度 | 设计依据 | 对系统的要求 |
| :--- | :--- | :--- |
| 平今、平昨与平仓指令 | 持仓归属、手续费归属、CTP 报单标志是三个相关但不同的概念。SHFE / INE 需区分今昨仓；各交易所的具体指令映射以柜台能力核验为准。 | 内部保留今昨仓及逐单冻结；按能力表映射报单，统一平仓指令也不能省略费用归属核算。 |
| 夜盘与交易日 | 夜盘所属交易日由交易日历确定，受节假日、品种和临时停市影响。存在日盘、夜盘、集合竞价及不同收盘时间。 | 使用版本化交易时段；分别保存物理时间与交易日，以交易日事件驱动今昨仓转换。 |
| 合约到期与换月 | 连续合约是派生研究序列，实际成交发生在有到期日的具体合约上。 | 保存当时可获知的主力映射，用真实合约价格、费用和保证金模拟移仓；纳入账户交割资格与最晚平仓日限制。 |
| 杠杆与逐日盯市 | 结算依据官方结算价，保证金还受账户、品种、合约、持仓组合和生效时间影响。 | 共享资金账本，区分盘中估值与正式结算，核算活动订单占款及盘中、盘后追保。 |
| 异常交易监控 | 监管限制、柜台流控和内部风险偏好有不同的口径与适用范围。 | 前置风控覆盖最终子订单和账户活动委托，保留规则版本；熔断过程可恢复、可追踪。 |
| 网络与进程故障 | 请求返回、柜台接受、交易所成交和回报到达并非同一个事件。 | 持久化订单意图与回报，按唯一事件记账；未知状态先对账，禁止盲目重发。 |

首版范围为 Windows、单账户、普通期货合约、面向日线 / 小时级信号的 Bar 事件驱动回测及 CLI 报告；首版目标策略类别为多品种低频 CTA，见 1.1。信号频率与执行价格的数据粒度分别配置，执行策略的数据要求见 6.1。先完成螺纹钢实际合约样例，再覆盖第二交割月及甲醇等跨交易所场景。期权、跨账户协同、组合保证金优化、GUI、遗传编程和低延迟交易后续按需求扩展；尚未实现的规则在运行配置中明确禁用。

<a id="section-1-1"></a>

### 1.1 目标策略类别与分层要求

策略类别决定数据分辨率、撮合模型、成本敏感度和验证方式，是所有后续设计的前置决策。阶段 0 的出口条件之一是在运行配置中声明目标策略类别；未声明类别的实验和账户不得进入模拟盘或实盘。

| 类别 | 典型持仓周期 | 首版必需的数据与撮合 | 可推迟的能力 | 必须上调优先级的事项 |
| :--- | :--- | :--- | :--- | :--- |
| 低频 CTA（日线 / 小时级，多品种） | 数日到数月 | 实际合约信号 Bar、结算价、涨跌停价，以及执行策略所需的时段开盘或盘中价格；Bar 撮合加固定跳数滑点 | Tick 录制、盘口排队、亚秒延迟仿真 | 执行时点及其数据覆盖、移仓价差、多品种跨周期样本、长假处理 |
| 分钟级日内 | 分钟到数小时 | 实际合约 1 分钟 Bar、当日实际涨跌停价、分时段成交量；Bar 撮合加参与率 | 盘口排队估算 | 平今费率历史、成交量参与率、延迟与执行校准 |
| Tick 级 | 秒到分钟 | 原始快照录制自阶段 0 开始、Tick 撮合、基于时间的延迟事件 | 无 | 排队估算校准、影子模式、报撤限制 |

首版目标类别为多品种低频 CTA。分钟级日内与 Tick 级策略的能力在本文中保留设计，按 9.1 路线在低频闭环验收后启用。类别声明写入实验清单，报告按类别列出所用撮合模型及其局限。

<a id="section-1-2"></a>

### 1.2 修订依据

本版综合交易视角与工程边界评审修订，评审条目并入各章正文。外部评审中的时段、接口及资金口径须核验后采纳，已有机制通过补充边界用例完善。修订原则：真实资金损失后果优先于回测收益失真，收益失真优先于研究效率；策略稳健性验证与真实成交校准是扩大资金的必要依据。实施采用薄切片路线：先以简化撮合得到第一条可对账的权益曲线并开始策略研究，再逐步补齐恢复、存储与精细撮合能力，见 9.1。

第二轮意见依据[《第二轮深度架构评审复核意见》](第二轮深度架构评审复核意见.md)及[《中国期货交易时间段与集合竞价机制详解》](中国期货交易时间段与集合竞价机制详解.md)整合。下表按对交易结果的影响列出要求与验收位置，13 个评审主题均落到现有业务模块；表中要求仍需实现和验证。

| 优先顺序 | 领域要求 | 正文与验收 |
| :--- | :--- | :--- |
| 1 | 在途订单三态、回报乱序、跨会话关联与迟到成交预占 | 4.1、4.6；A02—A04。 |
| 2 | 竞价撤单边界、日盘再竞价与只撤窗口、每日生命周期及半结算 | 4.2、6.1、6.3；A05、A21、A25 及其子用例、A26。 |
| 3 | 浮盈使用权限、逐日 / 逐笔盈亏视图及一致账户快照 | 4.4；A06、A08、A22。 |
| 4 | 历史合约代码、连续收益去除拼接差、主力映射与移仓解耦 | 4.3；A10、A29。 |
| 5 | 终端采集、客户端认证与目标柜台接入能力 | 第二章；A27。 |
| 6 | 单向依赖、行情驱动的全链路回放与事件恢复回放 | 3.4、6.6；阶段 2 依赖检查、A28。 |

时间底稿中的具体时点、历史调整日期及品种清单用于组织规则核验，不能替代原公告和历史版本存档；休市期报撤能力、维护窗口、终端采集顺序和浮盈权限分别取得适用规则或柜台证据。规则来源、核验日期和生效时间分别保存，未核验条目按第二章登记缺口，不因被评审引用就成为生产默认。

---

<a id="chapter-2"></a>

## 二、前期准备清单

### 1. 软件环境与依赖

- **Python**：先验证 Python 3.12 与候选 CTP 封装在目标 Windows 架构上的安装、导入、登录、查询和回报处理，再锁定具体版本。版本选择以实际 Wheel、DLL 和柜台兼容性为准。
- **依赖管理**：使用 `uv`，提交 `pyproject.toml`、`uv.lock` 和 Python 版本声明；记录 CTP API / DLL 版本及哈希。
- **数据处理**：`pyarrow` 负责 Parquet 与 Arrow 数据结构，优先选定 `polars` 或 `pandas` 作为主要计算接口，按需要引入另一者。
- **存储与报告**：`sqlite3` 管理事务和元数据，`duckdb` 做分析，`plotly` 输出报告，`structlog` / 标准日志记录结构化事件。
- **CTP 接入**：评估 `openctp-ctp`、`vnpy_ctp` 等成熟封装的适配能力；业务内核不依赖其具体对象类型，转换集中在网关适配器。

### 2. 账号、接口与规则核验

- **开发环境**：OpenCTP 可用于连接、回报和异常处理调试；端点是否全天候开放、如何定义交易日，以服务方说明为准。模拟环境的交易日和撮合规则需单独配置。
- **仿真验证**：在 SimNow 或期货公司指定仿真环境验证目标柜台行为。全天候模拟环境通过测试，不代表真实交易时段、费率或风控规则已经验证。
- **实盘准备**：按期货公司当前要求办理程序化交易相关报告、CTP 接入认证、终端信息采集和接口测试；保存要求版本及测试结果。账户、`AppID`、`AuthCode` 等凭证通过本地秘密配置加载，不进入代码仓库、日志或实验快照。
- **终端信息采集与认证**：按目标 CTP API / 操作系统 / 架构选择官方匹配的采集组件，记录发行包、库文件哈希及接入模式。`ReqAuthenticate` 用于客户端认证，不能将它等同于终端信息上报；直连与中继模式的采集、注册 / 提交及登录顺序，按对应版本文档和期货公司联调确定，涉及 `RegisterUserSystemInfo` / `SubmitUserSystemInfo` 时分别核验适用条件。采集载荷按 API 定义的字节及长度传递，不能手工拼接 MAC、IP 或用普通字符串处理破坏载荷；不在日志、实验快照或故障附件保存原始终端标识及采集密文。缺库、版本不匹配、采集 / 认证失败及错误返回须阻止进入可交易状态，对应 A27。
- **能力表**：记录交易所、品种、账户配置、CTP / 柜台版本、生效区间、来源和测试编号，至少覆盖开平映射、报单类型、有效期、分阶段报撤权限、休市预埋支持、查询限制、撤单口径、手续费、保证金及交割限制；区分本地受理、柜台接受与交易所受理的证据。

规则核验从交易所公开合约细则、交易管理办法及通知，以及 [CTP / SimNow](https://www.simnow.com.cn/) 对应发行包的文档和头文件入手。柜台维护时间、终端采集顺序及浮盈使用权限还须取得目标期货公司的说明和联调证据；本文中的时段、超时及资金示例不能替代生效规则文件。

### 3. 数据源与最小样本集

| 用途 | 数据要求 | 选型原则 |
| :--- | :--- | :--- |
| 原型与因子粗筛 | 日线或分钟线，明确主连与实际合约的区别 | 可评估 AkShare；仅有主连数据时，结果标注为粗筛，不作为真实成交及结算验收依据。 |
| 可对账的 Bar 回测 | 实际合约信号 Bar、执行时点对应的价格、官方结算价、涨跌停价、合约属性、日历及历史费用规则 | 分别核验信号与执行数据覆盖；日盘开盘、夜盘开盘和固定盘中时刻不能由同一个日线 Open 代替。先取得一个合约的完整小样本，再扩展到两个交割月。TqSdk、RQData 等按当前授权范围、历史长度与费用验证。 |
| Tick 仿真与实盘回放 | 原始快照、买卖价量、累计成交量、时间戳及来源说明 | 明确快照行情与逐笔成交 / 逐笔委托的差异，不能预设任一套餐提供全历史逐笔盘口。 |

下载前建立数据覆盖清单，逐项记录可用字段、时间范围、更新延迟、历史修订和使用许可。缺少结算价、关键费率或交易时段时，精确核算模式应中止并说明缺项；粗筛模式可使用显式假设，并在报告中列出影响。

执行价格覆盖需按 6.1 的执行策略单独校验。缺少所选时段或时刻的价格时，该执行策略不可运行；研究模式也不能静默借用其他时段的 Open / Close 或插值补造成交价，可保留不依赖该成交假设的因子粗筛结果。

### 4. 工程样本与研究数据集

工程验收样本与策略研究数据集分开采购和管理：

- **工程样本**：一个实际合约的完整小样本，含所选执行时点的价格、结算价、涨跌停价和规则历史，用于手工对账和 A 系列验收；补充集合竞价截止、日盘再竞价与只撤窗口、夜盘残留单、规则生效边界、节前停夜盘及小节休市样例，见 A25 子用例。半结算查询、跨日盈亏、郑商所短代码跨十年及主力切换价差另有独立样例。
- **研究数据集**：低频 CTA 至少覆盖 8 到 10 年、20 个以上活跃品种的实际合约日线，以及主力映射所需的逐合约成交量与持仓量，并跨越至少两个完整的商品牛熊周期。按执行策略另行补齐对应历史区间的时段开盘或盘中价格。单品种一年仅有数十笔交易，统计上不足以评价策略，策略验证必须在多品种组合上完成。
- 分钟级和 Tick 数据按策略类别及执行策略共同决定采购范围；低频信号若在固定盘中时刻执行，也须采购足以支持该时刻的价格数据。
- 测试品种组合：螺纹钢（SHFE，有夜盘）、甲醇（CZCE，跨交易所）、铁矿石或豆粕（DCE，市价单与跨期指令）、苹果或鸡蛋（无夜盘）、黄金或铜（凌晨收盘）；中金所品种只在确实要交易时加入。

### 5. 历史规则公告表

目标品种的手续费、保证金比例、涨跌停幅度、持仓限额及交割月规则的历次交易所公告，连同发布日期与生效日期，作为阶段 0 的独立交付物整理成生效表并登记来源。开仓、平今、平昨费率按品种、合约、账户及生效区间分别记录，注明按手或按金额计费、期货公司加收与舍入口径；不得套用交易所统一的平今费率倍数。历史平今费率变化会改变日内策略的可行性，不得用当前费率回填历史。

交易时段、夜盘开通或调整、日盘竞价制度、最后交易日特殊时段及节假日停复市公告纳入同一交付物。每条记录保存发布机构、标题 / 文号、原文位置、归档文件与哈希、发布日期、适用交易所 / 品种 / 合约、生效基准（交易日或物理时刻）、生效区间、替代关系、核验状态及对应测试编号。第三方汇总仅作为查找线索；维护和报撤能力还需关联期货公司说明及脱敏联调证据。

历史回测按所用区间取得旧、新规则，不能用当前交易时间页覆盖全部历史。网页核验日期不等于规则生效日期；缺少一手原文或无法唯一判断适用版本时，登记受影响品种、时段及实验范围，不伪造历史配置。初期按目标品种与研究区间整理，未启用交易所或期权功能不扩大首版交付范围。

### 6. 研究模式默认成本假设

研究模式不因缺失历史期货公司费率或保证金而中止，而是使用下表的显式假设并在报告中标注；精确核算模式仍按 4.4 的要求中止。

| 项目 | 默认假设 | 说明 |
| :--- | :--- | :--- |
| 滑点 | 主力合约每边 1 个最小变动价位，非主力合约每边 2 到 3 个 | 敏感性分析至少覆盖每边 0、1、2、3 跳 |
| 手续费 | 交易所公告费率，期货公司加收按交易所费率的固定比例假设 | 平今费率使用公告历史，不得用开仓费率代替 |
| 保证金 | 交易所公告比例加固定缓冲 | 保证金占用率上限作为内部风控规则，替代精确的期货公司保证金历史 |
| 成交量参与率 | Bar 成交量的固定比例 | 具体比例作为实验参数记录 |

这些数值是研究假设，不是任何品种的实际费率；进入模拟盘前必须以能力表和期货公司实际费率替换。

---

<a id="chapter-3"></a>

## 三、参考架构与共享交易内核

<a id="section-3-1"></a>

### 3.1 参考架构背景

原规划引用了 `HD95DUJbwAA5tej.jpg`。当前目录未包含该图片，以下保留原文中的架构描述作为背景，实施以本文件定义的接口和验收条件为准。

- 统一 CLI、运维脚本、可选 GUI 及回测入口。
- Strategy API、版本化数据加载器与快照加载器。
- Parquet 行情文件、DuckDB 分析、SQLite 元数据与不可变快照。
- 向量化信号、遗传编程因子研究、市场状态识别等研究子系统。

<a id="section-3-2"></a>

### 3.2 保留的设计

1. **分层存储**：Parquet 保存规范行情，DuckDB 查询已发布数据，SQLite 管理元数据及交易事务。压缩率、吞吐和内存占用通过代表性样本测试，不预设固定提升倍数。
2. **实验快照**：锁定数据、代码、规则、配置和依赖，使同一实验可以重放和审计。
3. **独立运维工具**：数据下载、校验、缺口检测、对账、回放及报告通过可重复执行的命令完成。

<a id="section-3-3"></a>

### 3.3 中国期货场景的适配

| 参考设计 | 本项目处理方式 |
| :--- | :--- |
| 永续合约 `get_funding_rates()` | 普通中国期货不使用永续合约资金费率模型。需要基本面策略时提供基差、期限结构和仓单接口，并保留数据发布时间。 |
| 向量化 `signal {-1, 0, 1}` | 保留为快速研究通道：基于实际合约按当时可见映射拼接的序列做向量化回测和参数扫描，使用第二章第 6 项的保守成本假设筛选；进入成交、资金及风控评估的策略再运行共享领域内核上的事件驱动回测。两条通道共用数据映射、执行时点及价格来源、成本口径。 |
| 离线研究入口 | 增加统一执行端口、CTP 适配器、持久化订单管理及恢复对账。 |
| GUI、遗传编程和复杂路由 | 在交易与数据基础验收后按需扩展。 |

<a id="section-3-4"></a>

### 3.4 Port/Adapter 与共享领域逻辑

统一策略接口之外，必须共享订单状态机、平仓路由、持仓与资金账本、规则查询、风控及移仓逻辑。`BaseEngine` 负责组织运行，禁止在回测和实盘中各自维护一套资金或持仓算法。

```text
CLI / scripts / 后续 GUI
          |
Strategy API: on_bar / on_tick / on_order / on_trade
              target_position(product) / buy / sell / short / cover / 查询当时可见数据
          |
共享领域内核
  OrderManager + SmartRouter + Positions + Ledger
  RuleBook + RiskController + RollManager + RecoveryCoordinator + DailyLifecycleManager
          |
  端口：Execution / MarketData / Clock / Journal / RuleStore / MappingStore / AccountQuery
          |
          +-- 回测适配器：历史数据、虚拟时钟、SimulatedGateway、内存 Journal
          +-- 实盘运行：独立执行服务、CTP 适配器、实时时钟、账户查询与回报
          +-- 持久化适配器：交易事件库、规则库、行情文件、实验清单
```

- 实盘由独立执行服务承载账户执行序列与共享内核，并独占该账户在本系统内的 CTP 交易连接。策略、移仓、看门狗及人工命令都提交给该服务，不直接调用交易 API；服务按 4.5 的控制者与控制代次校验所有交易操作。回测在进程内复用同一领域逻辑。
- CTP 回调负责转换和入队；同一账户的订单、持仓、账本与风控状态由一个执行序列更新，避免多个回调线程直接修改状态。
- 行情录制和分析使用独立工作队列，交易事件优先处理。队列有容量、积压指标和故障策略；订单或成交事件无法可靠保存时，禁止继续增加风险并告警。
- 回测使用虚拟时钟；行情可见、订单到达、成交和回报到达分别建事件。同时间事件的优先级与排序规则固定并纳入运行清单。实盘回放保留实际接收顺序。
- 策略只能查询 `available_at <= 当前策略时钟` 的数据，禁止通过直接读取数据湖获得未来信息。

#### 品种级策略接口与合约解析

- 策略层以品种为单位表达目标仓位或下单意图；共享内核按当时可见的主力映射和账户移仓状态，把品种意图解析为具体实际合约的开平子订单。策略也可显式指定实际合约，用于套利和移仓测试。
- 持仓查询同时返回品种净头寸和实际合约明细，明细含今昨仓。多策略共用账户时，同品种反向意图按策略归属分别记账、按账户净额路由；是否允许锁仓由账户配置决定，并在阶段 2 之前确定。
- 解析结果、所用映射版本和归属策略随订单意图一起持久化。

#### 回测模式的端口适配

- 交易事件库端口在回测模式使用内存适配器，运行结束时导出规范事件供快照与对账；SQLite 事务日志只服务模拟盘与实盘。
- 满足报价步长的委托 / 成交价格可用整数 Tick 表达；结算价、平均价及复权指标保留各自所需精度，不强制取整到报价 Tick。费用与盈亏用足够精度的定点数计算，在规定的记账环节舍入到最小货币单位，避免逐项提前取整；规则解析可使用 `Decimal`，热路径优化须通过 A06、A07、A15 验证。
- 性能优化不得改变舍入、事件顺序或交易语义。

#### 模块依赖约束

运行时的行情、意图和回报流向不等于 Python 的导入方向。按下表约束依赖，由入口装配具体实现，防止 `risk`、`smart_router` 与对象定义互相导入。

| 模块 | 允许的项目内依赖 | 边界 |
| :--- | :--- | :--- |
| `core/` | 本层无环依赖 | 纯类型、事件、常量与端口协议，不导入业务域或适配器。 |
| `domain/`、`strategy/` | `core/` 及各自模块内的无环依赖 | 领域层不读取数据湖或调用 CTP；策略通过协议查询数据、提交意图，不导入具体引擎。 |
| `data/`、`gateway/`、`infrastructure/`、`monitor/` | `core/` 及各自模块内的无环依赖 | 实现端口，通过注入的协议获取所需服务；网关归一化回报，不直接改持仓或账本。 |
| `engine/` | `core/`、`domain/` | 组织账户执行序列、生命周期和端口调用，接收注入的策略与适配器；独立执行服务放在本层。 |
| `scripts/` / CLI | 上述模块 | 唯一装配入口，选择运行模式和具体实现；其他层不反向导入入口。 |

领域组件可通过明确的值对象和无环调用协作，不要求每次内部调用都新增端口。共享规则、映射、时钟与持久化通过端口注入；依赖检查同时验证禁止边和循环依赖，不能仅以“模块可导入”作为通过条件。

---

<a id="chapter-4"></a>

## 四、核心业务逻辑与可靠性方案

<a id="section-4-1"></a>

### 4.1 平仓路由、订单状态与持仓冻结

#### 1. 以能力表映射开平意图

内部订单使用开仓、平今、平昨等明确意图，由适配器映射到实际柜台字段。SHFE / INE 的今昨仓路由需单独验证；DCE / GFEX / CZCE / CFFEX 也应逐项验证显式平今、平昨及统一平仓的支持情况、分配顺序和计费方式。不得仅凭 CTP 枚举中存在某标志就认定目标柜台支持，也不得把统一平仓等同于今昨手续费相同。

能力配置键至少包含 `exchange + product + broker_profile + ctp_version + effective_from`，并记录规则来源及实测用例。未知能力禁止实盘使用。平仓顺序由账户规则和策略配置决定，不能在网关中写死“所有品种优先平昨”。

能力表须逐项核验以下行为，保存目标期货公司、CTP / 柜台版本、生效区间、测试输入与脱敏结果；以下核验项不能作为所有柜台通用的既定事实：

- **撤单标识**：保存原订单的 `FrontID + SessionID + OrderRef` 与 `ExchangeID + OrderSysID`。跨会话优先使用已核验的交易所 / 系统编号路径；原订单三元组能否跨会话使用须实测，不得将新登录的前置、会话编号与旧 `OrderRef` 拼接。`OrderSysID` 尚未取得时等待回报、查询，或使用另一条已验证路径，不因超时盲目撤单或重发订单。
- **今昨仓查询**：核验 `YdPosition` 是日初量还是剩余量，以及 `PositionDate`、持仓方向、投机 / 套保标志的分组语义。若 `YdPosition` 为日初口径，不能直接当作盘中昨仓余量；仅在确认总仓与今仓均为同组剩余数量后，才用 `Position - TodayPosition` 推算。SHFE / INE 的今昨分条记录按实际返回口径归一化，禁止重复汇总，并与平今、平昨成交核对。
- **查询流控**：查询间隔、最大在途请求数、返回码与完成标志按目标柜台核验并配置。可从间隔 1000 ms、最多一个在途查询的保守配置开始测试，但不能视为通用限额或成功保证；遇到限流需退避，查询未完成不得当作完整快照。
- **前置维护**：按端点记录已知重启和维护窗口，重点验证结算前后及夜盘前的恢复；日常运维定期演练重连与对账，不假定每个前置每天都在固定时间重启。

#### 2. 持仓与逐单预占

按账户、实际合约、持仓方向和投机 / 套保标志维护 `pos_yd`、`pos_td`、`frozen_yd`、`frozen_td`；冻结量由逐单预占记录汇总，四个数量字段本身不构成订单状态机。

```text
available_yd = pos_yd - frozen_yd
available_td = pos_td - frozen_td
0 <= frozen_yd <= pos_yd
0 <= frozen_td <= pos_td
```

- `SmartRouter` 先生成不修改状态的子订单计划。账户执行序列对最终子订单统一检查，并在同一事务内记录订单意图、冻结持仓、预占保证金与费用。
- 开仓预占按未成交部分的保守成交价格计算；平仓单也需预留可能的手续费及组合保证金变化。子订单的报撤额度分别计数。
- 每笔去重后的成交只更新一次持仓、冻结与账本。成交后将相应预占转换为实际占用，剩余活动数量继续预占。
- 撤单或拒单只释放已确认不会成交的部分。撤单请求发送或接口返回成功，不表示原订单已经撤销。
- 若终态订单回报中的累计成交量大于已入账成交量，保留差额对应的冻结，等待迟到成交或查询确认；不能因先收到终态就全部解冻。

#### 3. 订单状态与回报语义

| 状态或事件 | 必须执行的处理 |
| :--- | :--- |
| `CREATED / SUBMITTING` | 提交前持久化意图与预占，分配稳定的 `client_order_id`，保存父子订单关系。 |
| `ACCEPTED / PARTIALLY_FILLED` | 保存柜台订单标识与累计成交信息，按唯一成交记账，不依靠回调到达顺序推断全部事实。 |
| `FILLED / CANCELED / REJECTED / EXPIRED` | 作为已确认终态；迟到状态不能使订单倒退，迟到但尚未入账的真实成交仍需处理。 |
| `cancel_pending` | 作为独立撤单状态，可与部分成交并存。撤单拒绝后原委托可能仍有效，预占继续保留。 |
| `UNKNOWN` | 发送、连接或恢复过程中无法确定柜台结果；保留保守预占并进入对账，不把超时自动转换为拒单。 |
| 明确未发送的本地失败 | 记录失败原因并释放相应预占；能否认定“未发送”必须由适配器的明确返回语义决定。 |

网关须覆盖本地调用返回、`OnRspOrderInsert`、`OnErrRtnOrderInsert`、`OnRtnOrder`、`OnRtnTrade` 及撤单错误回报，将其转换成统一领域事件。同一事实通过多种回调重复到达时，业务结果不重复计算。

`CTPFeedbackNormalizer` 负责字段校验、标识关联与事件归一化，具体账本变更仍由共享内核执行：

- 本地意图及原会话订单映射必须在报单前存在。成交先于报单回报时，优先使用已核验作用域内的 `ExchangeID + OrderSysID` 或可唯一关联的原会话订单映射绑定；`OrderRef` 不能单独作为跨会话唯一键，成交回报未携带原会话信息时也不能补上当前会话号猜测关联。
- 无法唯一归属的真实成交先持久化为待关联事件，并建立带远端标识的占位记录。在确认本地订单关联或核实为外部成交前，不猜配、释放其他订单的预占，不发布受其影响的可交易账户快照。确认后在同一账户执行序列中一次性入账、合并占位并重建相关预占；仅策略归属仍未知的保留“未分配策略”标签，差异处置完成前禁止新增风险。不能因缺少本地订单或 `OrderSysID` 映射丢弃成交，也不能擅自按外部委托补开仓。
- 迟到报单回报补全占位记录与原意图的关系，只合并标识、归属及预占，不再次生成成交记账。订单终态与累计成交事实分开维护，`CANCELED` 后仍可补记迟到成交，不因迟到的“已报”回报退回活动状态；无法解释的超量或归属冲突进入对账。对应 A02、A04。

#### 4. 发送状态与在途恢复

`send_state` 与订单成交 / 撤销状态正交，保存每次发送尝试及证据：

| 发送状态 | 判定依据与处理 |
| :--- | :--- |
| `NOT_SENT` | 适配器能证明尚未调用发送，或已核验的返回语义明确表示请求未送出；记录失败后释放相应预占。不能把所有异常或非零返回统一归为未发送。 |
| `SENT_UNKNOWN` | 本地调用已受理但缺少远端确认，或发送结果本身不确定；保留预占，禁止盲目重发，交由恢复协调器核对。`ReqOrderInsert` 返回 0 不代表柜台接受或成交。 |
| `CONFIRMED_REMOTE` | 已有可关联的远端订单、成交或明确拒绝证据；按实际订单结果推进状态，远端确认不等于报单成功。 |

重连后按查询能力及流控立即开始核对，保存查询范围、交易日、完成标志与回报合并水位。告警与人工介入超时按柜台配置；30 秒可作为测试参数，不能作为通用最短挂起时间或“订单不存在”的证据。超时、一次空查询或查无此单均不自动释放未知预占或重发；只有确认未发送 / 未受理或核实远端终态后才关闭未决状态。对应 A03、A04。

<a id="section-4-2"></a>

### 4.2 交易日历与结算事件

1. **交易时段表**：按交易所、品种、合约例外和生效区间记录交易日、竞价、连续交易、休市、夜盘跨日、节假日及临时停市。区分品种通常支持夜盘与某交易日实际存在夜盘；最晚交易日和最后交易日的特殊时段也纳入规则。
2. **交易日映射**：由具体时段映射到 `trading_day`。普通非节假日周五夜盘通常归入下一周一交易日，但不能用“自然日期加一天”或“下一个工作日”代替期货日历；周六凌晨的夜盘仍属于对应交易时段。
3. **来源校验**：保留 CTP 的 `TradingDay`、`ActionDay` 等原始字段，并结合端点规则与日历校验后生成统一时间。模拟端点使用独立配置，不默认继承实盘日历。
4. **收盘事件**：按合约适用时段生成 `SessionClosed`；不能统一写死 15:00，例如中金所国债期货常规收盘时间为 15:15，具体历史时段仍按版本化日历执行。
5. **结算事件**：官方结算数据经完整性检查后生成带交易日及版本的 `SettlementReady`，驱动账本结算。数据尚未就绪时标记 `SETTLEMENT_PENDING`，不得静默用收盘价替代。
6. **今昨仓转换**：在已确认的 `TradingDayAdvanced` 事件中完成一次性转换，并核对待结算状态、上一交易日订单及冻结。晚到回报按其原交易日处理；跨日订单的失效或继续有效遵循柜台规则，不能直接清空全部挂单。

行情到达下一交易日时继续采集，但上日结算或账户对账未完成，应阻止新增风险。结算修订通过有版本的差额更正事件处理，不覆盖旧账，也不重复计入收益。

#### 交易小节、集合竞价与操作权限

`MarketPhase` 按实际合约维护竞价申报、竞价撮合、只撤不报、等待连续交易、连续交易、小节休市、停牌、收盘及未知状态；阶段与 `session_id` 分开，不为每个日夜盘小节复制一套枚举。申报、撮合属于集合竞价，等待与连续交易属于后续市场阶段；并非所有品种都必经同一流程，也不人为规定固定长度的等待阶段或为每次小节恢复补一次竞价。

每个阶段分别配置 `can_submit()`、`can_cancel()`、`can_match()`，包含订单类型、受理层级及生效规则。下表开盘竞价的申报阶段按 `true / true / false`、撮合阶段按 `false / false / true` 核验；只撤不报窗口明确为 `false / true / false`，不能把开盘前五分钟视为整段可撤单时间。时段边界按左闭右开区间处理，截止时刻已经进入下一阶段；撮合窗口允许执行规定的竞价撮合，不表示该分钟内可按每个 Tick 连续撮合。

下表按时间底稿列出常规核验基线，时区为 `Asia/Shanghai`。生产及历史精确核算使用第二章要求的一手规则与已核验版本；其中历史调整日期仍须关联原公告，新增或特殊品种另行配置。

| 适用场景 | 时段及操作要求 | 版本与边界 |
| :--- | :--- | :--- |
| SHFE / INE / DCE / CZCE / GFEX 商品日盘 | 常规连续交易为 09:00–10:15、10:30–11:30、13:30–15:00；小休与午休不撮合，报撤权限单独核验。 | 五个商品交易场所均有常规 10:15–10:30 小休，豆粕、甲醇不能按该时段连续交易建模。 |
| 无夜盘商品的日盘开盘 | 08:55–08:59 申报、08:59–09:00 撮合，09:00 进入连续交易。 | 仅为实际适用的日盘开盘竞价生成事件；10:30、13:30 恢复交易不自动补竞价。 |
| 实际开设的夜盘开盘 | 20:55–20:59 申报、20:59–21:00 撮合，21:00 进入连续交易。 | 夜盘是否存在及 23:00 / 次日 01:00 / 02:30 等收盘分档按品种和历史版本配置，不从交易所名称统一推导。 |
| SHFE / INE / DCE 有夜盘品种的日盘 | 08:55–08:59 再次申报、08:59–09:00 撮合；仍有效的夜盘未成交申报按规则参与。 | 时间底稿记载自 2023-05-26 交易日起调整；旧、新规则分别存档，不将新日盘竞价回填到此前历史。 |
| CZCE 有前序夜盘的常规日盘 | 08:55–08:59 只允许撤销夜盘未成交单，不接受新报单、不撮合；08:59–09:00 等待，09:00 连续交易。 | 独立于三所日盘再竞价模板；节后等实际无前序夜盘场景使用相应公告覆盖，不能永远只撤不报。 |
| CFFEX 股指与国债期货 | 09:25–09:29 申报、09:29–09:30 撮合；09:30–11:30 连续交易，无 10:15 小休；13:00 午盘，股指到 15:00，国债常规到 15:15。 | 仅在启用中金所时适用；不能套用商品 08:55 / 09:00 开盘及 13:30 午盘模板。 |
| CFFEX 国债历史及最后交易日 | 时间底稿记载 2020-07-20 起上午开盘由 09:15 调整至 09:30，开盘竞价随之由 09:10–09:15 调整至 09:25–09:30；最后交易日仅上午交易。 | 历史样例只使用当时已挂牌合约并匹配适用细则；最后交易日下午不生成可撮合时段。 |

夜盘残留单按订单有效期、交易日及阶段转换规则决定进入日盘竞价、继续等待或失效，保存原订单标识、累计成交和剩余预占。日夜盘切换不重建同一订单、不重复预占或计费，也不自动把仍有效委托清空；失效、撤销及迟到成交继续按 4.1 的确认语义处理。

节前停夜盘及节后恢复直接依据交易所放假公告生成实际时段，不能用“假日前一自然日晚上”或普通工作日公式推算。例如长假从周一开始、周五为最后交易日时，仅关闭周日夜盘会漏掉周五的取消安排。若公告规定节后首日日盘集合竞价，应覆盖常规夜盘品种的日盘模板，尤其验证 CZCE 的只撤不报窗口；覆盖关系、适用交易日与原公告一起保存。

GFEX 当前无夜盘及其规则预留按时间底稿登记，实际接入时复核最新公告；未来开通夜盘须重新确认开通品种和日盘竞价办法，不能由现行预留条款自动启用。期权属于后续扩展，若启用中金所股指期权，另行核验 14:57–15:00 收盘竞价的报撤、价格形成及订单延续规则，不照搬开盘 4+1 分钟模型。

日历与 `OnRtnInstrumentStatus` 等可用状态回报共同确定实时权限，记录来源、接收时间和有效范围；状态缺失、过期或冲突时采取已配置的保守门禁并告警，不能用一条全账户的“开市”布尔值覆盖所有品种。区分本地保存意图、柜台预埋、交易所报单 / 撤单受理与实际撮合；休市期预埋或撤单能力须单独核验，预埋受理不等于可撮合订单，不能从“不撮合”推导所有请求都会被拒绝。

本地发送前与模拟到达时分别检查权限。若撤单在申报窗口发出、在禁止撤单阶段才到达，应产生撤单拒绝并保留原单及预占，之后的竞价成交正常入账；可配置截止前的安全缓冲，但不能保证请求一定赶到。没有竞价数据时，Bar 模式不得凭连续交易 Open 宣称复现了集合竞价成交；按执行策略拒绝该撮合假设或显式改为连续交易后的可观测价格。对应 [A25 子用例](#acceptance-a25)，时段合法性对所有策略适用，已启用范围须逐项验证。

#### 每日生命周期协调

`DailyLifecycleManager` 编排日常检查、结算、维护、重连和恢复，复用 4.6 的恢复协调器。按端点及账户维护 `PRE_OPEN / RUNNING / POST_CLOSE / SETTLEMENT_IN_PROGRESS / MAINTENANCE / RECONCILING` 等运行阶段；它与合约交易阶段、连接状态、风控状态分别管理，不能压成一个全局状态机。维护与结算时间是带来源的预计窗口，实际转换还依赖连接、结算及查询证据，不写死每天 15:30 断开、18:30 恢复或把 20:55 当作连续交易开盘。

- 某品种收盘不等于账户所有品种均已结束交易；按所选合约时段及柜台信号安排收盘任务，避免商品 15:00 收盘时误停仍在交易的国债期货。
- 结算中收到的资金 / 持仓查询保存为待核对原始快照，附交易日、查询批次及完整性标记；持仓已切换、资金未结转时不能覆盖最后一致的可交易账户快照。登录成功或结算确认应答本身不能证明新交易日数据已完整。
- 结算未完成或快照不一致时禁止新增风险；行情采集、回报处理、对账、告警及基于最后一致状态的保守风险评估继续运行。不可把“禁止使用半结算数据”实现成关闭风控；撤单 / 减仓仍须通过 4.5、4.6 的控制权、状态及资金检查。
- 仅在结算单所属交易日与完整性核验、必要的结算确认、账户查询及增量回报核对完成后，才发布一致快照并申请恢复。任务按账户、交易日、事件类型及版本幂等执行，重启不重复结算、转换今昨仓或重置计数；越过预计窗口仍未就绪时保持门禁并告警。对应 A26。

<a id="section-4-3"></a>

### 4.3 主力映射、连续合约与真实移仓

#### 1. 当时可获知的主力映射

- 主力判定使用决策时点已经可用的成交量、持仓量及合约资格数据。若使用 T 日完整日盘数据，默认不早于 T+1 交易日首个可交易时段生效，并要求输入已达到 `available_at`、决策已完成；若数据迟到则顺延，不能回填为 T 日已交易的主力。
- 保存 `decision_time`、`effective_from`、候选合约、输入数据版本及选择规则；并列、流动性门槛、连续确认天数和避免频繁切换的条件必须确定。
- 对实际合约使用稳定标识，包含交易所及完整交割年月；保留原始柜台代码，避免简称、大小写或郑商所短年份代码在多年数据中混淆。

郑商所短年份代码通过 `ContractResolver` 解析。例如 `TA405` 必须结合行情记录所属交易日、数据源说明及当时有效的合约目录，解析到含完整年份的内部标识，如 `CZCE.TA.202405`；`TA2405` 仍只是常见的两位年份供应商别名。不得按程序运行当天年份解释历史文件，也不能仅用 `current_year ± 1` 代替合约上市 / 到期区间。候选范围由具体品种的挂牌规则与合约元数据限定；跨年、跨十年或缺少日期导致无法唯一解析时隔离并报错，不猜测归属。保留原代码、来源日期、解析依据与目录版本，对应 A29。

#### 2. 研究序列与执行价格

- 策略可通过价差平移或比例复权计算连续指标，但每个时点只能使用当时已生成的映射和复权因子。禁止将回测结束后生成的整段后复权历史直接用于所有历史决策。
- 原始拼接价、连续指标价与收益率视图分别定义。切换日不能把“新合约今日收盘 / 旧合约昨日收盘 − 1”当作可交易收益；用于动量的收益率可按当时选定同一实际合约的相邻价格计算，或采用经过验证的连续收益构造，并保存两端合约、时刻、调整因子及可见时间。
- 价格平移与比例复权的适用指标不同，价差平移后的价格百分比变化不能直接当作真实资金收益。不能简单把所有切换日收益置零或删除交易日；缺少调整参考价时明确标记、禁用受影响信号或采用已声明的降级方案。A10 包含“新旧合约各自价格不变但存在价差”的样例，确保没有由拼接产生的动量或账本盈利。
- 策略发出的意图须解析为明确实际合约；成交、费用、持仓估值、涨跌停和保证金均使用该合约未经连续复权的原始价格。
- 数据查询接口区分实际合约与连续序列，保存复权方法、参考价及版本。缺少真实合约数据时，不输出可与实盘对账的资金结果。

#### 3. 主力识别与账户移仓解耦

`data/dominant_contract.py` 只产生带可见时间及生效时间的主力映射；`data/continuous.py` 据此生成指标 / 收益率视图。`domain/rollover.py` 中的 `RollManager` 通过映射查询端口消费已发布版本，结合账户实际持仓和策略参数独立执行移仓，不自行扫描数据文件或重算主力。映射变化不自动修改仓位，两者分别记录并分别报告。

- 主力识别以当时已知的品种活跃月份结构为先验，按品种和历史生效区间配置；1、5、9 月或 1、5、10 月只是部分品种的候选示例，不能统一套用。在候选合约范围内按成交量与持仓量判定，并要求连续确认天数，避免逐日抖动。
- 移仓时点作为策略参数，可选主力切换生效日、已确认持仓量交叉后的第 N 个交易日（N >= 0），或已知最晚平仓日前 N 个交易日，并允许分批执行。输入须满足 `available_at <= decision_time`；保存触发事实、决策时间、规则与日历版本，不得从未来实际交叉日倒推提前移仓。
- 若后续引入交叉日期预测，须保存决策当时生成的预测、模型及输入版本；未来实际交叉日期不能替代历史预测。规则迟到时按当时状态处理，不回填此前的移仓决策。
- 腿序可配置：保证金充足且临时敞口符合账户限制时可默认先开新仓再平旧仓；否则按配置先平后开或暂停。两腿之间的临时双倍敞口、空仓窗口及部分成交风险均计入风控评估。

#### 4. 两腿移仓状态机

```text
PLANNED -> LEG_1 -> LEG_2 -> DONE
                   任一阶段异常 -> PAUSED / FAILED（保留已成交事实）
```

每条腿均经过订单管理与风控，并处理部分成交、撤单、重试上限、涨跌停、交易时段及保证金不足；未完成移仓时报告实际剩余持仓和风险暴露。

移仓的实际资金损益来自真实两腿成交及后续持仓变化，手续费逐笔计算，滑点体现在成交价格中。不得额外固定扣除“双倍手续费＋两倍滑点”，以免重复计费。报告将新旧合约价差、执行费用与滑点、持仓收益及注明基准的展期收益归因分别列示；合约间价差本身不是新增现金损益，不得再记入账本。临近交割时依据账户资格及期货公司最晚平仓要求提前触发处理。

<a id="section-4-4"></a>

### 4.4 资金账本、保证金与追保

#### 1. 统一记账口径

- 建立不可变的成交、费用、资金划转、结算和差额更正记录。账本由事件推导，策略收益不包含入金、出金等外部现金流。
- 今仓初始计价基准为成交价，昨仓为上一结算价。平仓按所平持仓当前计价基准确认盈亏；日终只对剩余持仓计提到官方结算价的盈亏，并更新下一交易日基准。
- 盘中动态权益由已结余额与未实现盈亏等账户项目计算。原始开仓价可用于交易绩效分析，但已经划入余额的每日结算盈亏不能再次加入动态权益。
- 报单价格比较优先转换为整数 Tick；结算价、均价、金额和费率保留所需定点精度或使用 `Decimal`，按账户规定的计费粒度及舍入规则核算。与柜台比较时明确最小金额单位和允许误差。
- 最新价格过期、盘口缺失或处于涨跌停流动性不足状态时，记录估值质量并使用配置的保守风险估值，不将长期未变化的最新价当成可立即平仓价格。

共享一套成交与结算事实，生成两种盈亏视图，不维护两套可各自增加余额的账本：

| 视图 | 计算基准 | 用途 |
| :--- | :--- | :--- |
| `mtm_close_pnl`（逐日盯市平仓盈亏） | 今仓按实际开仓价，昨仓按上一结算价；分别计算多空方向、乘数与对应平仓数量。 | 日常结算对账，并结合持仓盯市、费用、冻结和账户规则计算权益与风险。 |
| `trade_close_pnl`（逐笔交易平仓盈亏） | 平仓价与所匹配原始开仓批次的价格比较，跨日保留开仓成本及费用分摊。 | 扣费后的胜率、盈亏比、平均持仓时间及策略归因；配对规则、部分平仓与策略归属须固定。 |

手工验收示例：无手续费、1 手多仓、乘数 10，100 开仓，当日结算 110，次日 108 平仓。首日结算入账 +100，次日盯市平仓 −20；逐笔平仓盈亏为 +80，最终权益合计增加 80，不能再把逐笔 +80 叠加到账本。夏普、回撤、月度收益使用包含未平仓估值并剔除外部现金流的时间序列权益；不能只用已平仓交易盈亏代替。对应 A06、A22。

#### 2. 保证金与可用资金

账户模型需区分持仓保证金、活动订单的增量保证金、待扣费用和其他冻结，并避免把柜台已包含的冻结重复扣减。模型与柜台余额、权益、可用资金等字段的映射必须通过对账样例确认。

浮盈能否参与开仓、风险度或保证金覆盖，按交易所结算口径、期货公司、账户权限及生效规则核验，不能统一断言“浮盈一定能开仓”或“一定不能开仓”。至少明确以下字段：

- `broker_available`：柜台查询的原始可用资金，附交易日、查询时点、来源与一致性状态；先核验其是否已包含浮盈浮亏、保证金及冻结，不能再次重复扣减。
- `available_for_new_trades`：按该账户开仓资金规则得到的额度，同时检查现有活动订单的增量需求。盈利可计入比例、浮亏扣减、按合约还是账户汇总、费用与冻结的处理均版本化；不能直接把动态权益当作开仓额度。
- `margin_coverage_equity`：用于保证金覆盖及风险度评估的权益口径，注明分子与所比较保证金的定义。它与开仓额度是同一账户的不同视图，不能相加作为两笔可用资金，也不能因计入浮盈就直接减去法定保证金需求。

研究模式缺少历史浮盈使用规则时，可显式采用“浮盈不增加开仓额度、浮亏按保守口径扣减”的假设并做敏感性分析；已通过结算划入余额的盈利须按新交易日规则处理，不能永久当作未实现浮盈扣除。实盘及精确核算模式以已核验的账户规则和一致查询为准，差异未解释时阻止新增风险。A08 同时覆盖允许 / 不允许浮盈开仓、浮亏、结算后盈利转入余额及冻结重复扣减的情形。

单向大边或组合优惠由账户、品种、合约组合、方向、投机 / 套保标志及历史生效规则共同决定，不能仅按交易所名称套用 `max(多头, 空头)`。只有已验证的组合规则才启用优惠；锁仓单边平仓可能改变优惠，减仓前也要重算资金需求。

缺失保证金规则时，精确核算模式中止；研究模式按第二章第 6 项使用交易所公告比例加固定缓冲，并以保证金占用率上限作为风控规则。风控同时评估现有持仓及所有活动委托可能成交后的资金需求。

#### 3. 追保与强平

按期货公司规则建模盘中和盘后保证金检查、追保期限、资金到账以及强平处理，不假定所有账户均等到次日开盘集合竞价才强平。强平委托也受时段、涨跌停、流动性和柜台限制约束；无法成交时保留真实仓位、持续告警并报告未解决风险。

#### 4. 结算单对账

期货公司每日结算单是账户资金、持仓、盈亏与费用的权威口径，柜台实时查询只是中间状态。系统交付结算单解析器，解析逐日盯市与逐笔对冲两种平仓盈亏、手续费、保证金占用及持仓明细，并在每个交易日结算确认后自动与本地账本逐项比对，输出差异报告。差异超过约定误差时禁止新增风险，直到人工确认或更正事件入账。解析器对样例结算单的结果纳入 A22 验收。

<a id="section-4-5"></a>

### 4.5 事前风控与熔断执行

风控是所有策略、移仓、看门狗及人工交易指令的必经路径。独立执行服务按账户串行执行控制权核验、风控校验和预占，网络发送在本地事务提交后进行；控制权切换与发送使用同一执行序列，避免校验通过后被旧控制者继续发单。

```text
Strategy / RollManager / RiskReduction / Watchdog / 人工控制发出意图
     |
SmartRouter 生成最终子订单计划（不修改账户状态）
     |
账户执行序列：核验控制者与代次、规则、价格数量、自成交、资金、敞口、报撤额度
     |
同一事务：保存订单意图 + 冻结持仓 / 资金 + 预占报单额度
     |
ExecutionPort 发送前复核控制代次 -> 统一回报事件 -> 去重并更新账本与预占
```

#### 1. 三类规则分别管理

| 规则类别 | 配置要求 |
| :--- | :--- |
| 交易所及监管要求 | 记录来源文件、发布日期、生效时间、账户或实际控制关系范围、计数单位及例外；以当前适用版本核验。 |
| 期货公司及柜台限制 | 记录请求速率、查询限制、报单类型、撤单口径、保证金和账户权限；通过指定柜台测试。 |
| 内部风险偏好 | 配置品种与账户资金上限、活动订单敞口、亏损 / 回撤指标、行情过期阈值及风险状态转换。 |

撤单总数、撤单率、频繁报撤次数和持仓比例不设置无来源的生产默认值。日内回撤需明确基准、峰值更新及现金流调整方法。不同规则分别定义统计窗口、分母、重置交易日，以及请求、接受、撤销或成交哪个事件触发计数；计数随事件持久化，重启不得清零。

交易所硬约束至少包含：中金所品种的日内开仓限额和撤单次数限制；各交易所按合约阶段递减的一般持仓限额；自然人客户在多数商品品种进入交割月前的最晚平仓要求；上市首日及连续单边市后的涨跌停幅度与保证金调整。这些约束在共享内核中执行，回测与实盘使用同一实现，并在阶段 3 的回测验收中生效，使策略违规在回测报告中可见，不能推迟到模拟盘才发现。对应验收为 A19。

#### 2. 自成交与账户范围

检查同合约反向活动订单价格是否可能交叉，并包含正在发送、撤单未确认及未知状态委托。风险校验和活动订单登记在同一账户执行序列中完成，避免两个并发意图分别通过检查。

首版要求账户交易入口受控，重连时同步人工或其他终端产生的委托。发现无法识别的活动订单或持仓差异时禁止新增风险。涉及多个实际控制关系账户时，需要统一账户视图或柜台侧控制，不能声称单进程本地挂单簿已覆盖全部监管范围。

#### 3. 熔断过程

```text
NORMAL -> REDUCE_ONLY -> CANCELING -> FLATTENING -> HALTED
```

- 触发熔断后先禁止新增风险，再按规则撤销冲突或增加风险的挂单；在撤单回报与迟到成交核对后，按实际剩余仓位分批减仓。
- 撤单和减仓可获得高于普通开仓的调度优先级，并预留可用请求预算，但仍须遵守监管、柜台硬限制、真实可平仓量和资金检查。
- 自身普通开仓冷却条件不应阻断合法减仓。内部限制应按增加风险、撤单和降低风险分别配置，禁止为“紧急清仓”绕过全部校验。
- 行情过期、断线、涨跌停或限流导致无法清仓时，保留未完成状态、剩余风险、重试上限和告警；不能把发出清仓请求等同于仓位归零。
- 进入 `HALTED` 后仍处理回报、对账和监控；恢复交易前要求异常原因消除、账户一致且风险状态明确重置。

#### 4. 长假前降风险钩子

交易日历标记长假及其前后交易日，并关联交易所保证金和涨跌停调整公告。风控提供节前若干交易日按比例减仓或降杠杆的钩子，参数按品种与策略配置；复市后的恢复条件明确定义。回测中同样执行，使历史收益反映节前处理的成本。

#### 5. 看门狗与失联处置

运行在应用进程内的止损与熔断逻辑随承载进程退出而失效。不能仅凭 CTP 字段或枚举推定存在交易所 / 柜台托管止损；触发地点、有效期及断线后的存续能力须按目标柜台核验，未核验时按无托管保护设计。

- **唯一交易出口**：独立执行服务是本系统唯一持有该账户 CTP 交易连接的进程，实施单实例约束。策略、看门狗与人工控制通过该服务操作，不各自登录交易接口。服务持久化当前 `controller_id` 与单调递增的 `control_epoch`，对开仓、平仓 / 减仓、撤单、改单及恢复交易命令，在接收和最终发送 / 生效前校验控制权；所有系统内的发送路径均不得绕过。
- **失联不等于退出**：独立监控进程检测策略心跳。超时后的默认处置须配置为告警等待人工，或申请接管后撤单 / 减仓；心跳超时本身不授予交易权。暂停后恢复或网络恢复的旧控制者仍持有过期代次，其命令必须被拒绝。
- **接管顺序**：执行服务先停发旧控制者命令，在账户执行序列内持久化新控制者及新代次，再拒绝旧代次排队命令；明确未发送的请求按失败事件释放预占。随后执行 4.6 对账，核对此前已发送及发送结果未知的请求，一致后才允许新控制者按配置撤单或减仓。已提交到柜台的请求不会因代次变更消失，其预占与迟到回报继续处理；接管过程写入交易事件库。
- **执行服务故障**：执行服务重启或控制者变更时，先恢复已持久化的控制记录，再推进代次，拒绝此前的控制命令；单纯 CTP 连接重连不自动切换控制者。从备份恢复不得回退或复用已使用代次，无法确认最新控制记录时停止自动接管。若旧交易出口不能被确认停止或经独立机制隔离，不得仅凭心跳超时另建交易连接接管，保持禁止新增风险并告警，由人工按已验证流程处置。
- **整机故障与备份保护**：同机看门狗不能覆盖主机断电或整机网络失联。部署独立于交易主机的存活告警，并验证人工应急处置路径；期货公司条件单等托管保护经能力表核验后可作为备份，其触发、失效及与本地订单冲突的处理须测试，不视为必定能平仓。
- **运行环境**：禁止交易时段自动重启与睡眠，将 Windows 更新安排在维护窗口并做重启后检查；部署位置结合到目标前置的实测网络质量选择，可优先评估期货公司同地域的托管服务器。本地开发机不承担实盘运行。
- 对应验收为 A23。

#### 6. 人工命令与开盘检查

提供命令行级别的人工控制：暂停新增风险、撤销全部挂单、按品种或全部减仓、查询当前状态与差异、确认差异后恢复。影响交易状态的人工命令须由当前控制者执行，或完成上述接管后执行；均经过唯一执行服务、同一风控与记账路径并留审计记录，不能通过人工命令绕过控制代次校验。

每个适用交易时段开盘前，按版本化日历与配置的提前量执行检查清单：唯一执行服务及控制代次、连接与登录、交易日与结算确认、行情时效、规则版本、账户对账状态、监控及外部告警可用性。08:55 / 20:55 仅是部分常规时段提前五分钟的示例；节假日、取消夜盘及特殊开盘时间以日历为准。开盘时仍处于对账中的账户保持禁止新增风险，并告警。

<a id="section-4-6"></a>

### 4.6 断线恢复、事件去重与对账

#### 1. 持久化与幂等

- 使用独立交易事件库保存 `client_order_id`、父子订单关系、账户 / 策略归属、`controller_id` / `control_epoch`、`send_state`、柜台标识映射、待关联成交、规范事件、逐单预占及递增 `journal_seq`。交易回报保存必要的脱敏原文，终端采集载荷及凭证不落盘；旧代次请求的真实成交仍按订单标识入账，不能当作过期命令丢弃。
- 成交去重键由适配器按柜台编号的真实唯一性范围定义，通常需要账户、交易所、交易日及成交编号等组合；必要时包含合约或方向。不能只用 `OrderRef` 或 `TradeID` 单字段跨会话去重。
- 事件去重、账本更新、预占变更和处理游标在同一事务提交。快照保存对应 `journal_seq`，恢复时从明确游标重放，避免“快照加全部历史成交”重复入账。
- 本地数据库事务不能与远端 CTP 报单原子提交。“意图已保存但是否发送未知”以及“柜台已接受但回报丢失”均须进入核对流程；重新使用同一个本地编号不等于柜台会自动去重。

#### 2. 启动与重连流程

```text
DISCONNECTED -> RECOVERING -> RECONCILING -> READY
                                  |
                         差异未解决：保持禁止新增风险
```

1. 先确认执行服务单实例及旧出口隔离状态，恢复持久化控制记录；服务重启或接管时按 4.5 推进代次并拒绝旧命令，单纯重连不自动授权另一个控制者。加载已提交快照与后续事件，恢复订单、预占、持仓、账本和报撤计数，将无法确认远端结果的请求标记为未知。
2. 按已核验的接入模式完成终端采集 / 注册、认证、登录及必要的结算确认，顺序以第二章的能力表为准；结合生命周期检查交易日与结算完整性，重新订阅行情。按查询间隔、在途上限和退避规则查询资金、持仓、订单与可获得的成交记录，保留原订单标识供跨会话核对与撤单。
3. 查询期间缓存并按序保存实时回报。订单、成交、持仓和资金查询通常不是同一原子快照，需依据查询完成标志、所属交易日、结算状态、游标和重复查询建立一致视图，避免先覆盖持仓再重放相同成交；半结算或跨交易日混合的查询不能发布为可交易快照。
4. 识别外部委托与未知发送结果，按唯一事件重建预占，输出逐项差异。若柜台查询范围不足以确认状态，保持未知并提供可人工核对的记录，不推测已拒单后重发。
5. 在唯一交易出口、当前控制权、交易连接、行情时效、交易日、待结算状态、订单、持仓及资金全部通过一致性检查后切换为 `READY`。`READY` 不自动解除既有熔断或暂停状态；未解决差异、恢复超时和重复断线均须告警。

验收需注入柜台接受后断线、重复及乱序回报、部分成交后进程终止、查询中继续成交、磁盘写入失败等故障。恢复成功以柜台状态与本地账本一致为准，连接重新建立本身不构成成功。

---

<a id="chapter-5"></a>

## 五、数据存储与实验版本管理

<a id="section-5-1"></a>

### 5.1 行情分段录制与可靠发布

首版历史 Bar 数据直接写入版本化 Parquet。接入实时行情后，采用短周期完整文件与清单发布机制；标准 Feather / Arrow IPC 文件依赖完整文件结构，不承担可任意追加、断电后自动恢复的 WAL 承诺。

```text
原始行情 -> 独立有界队列 -> 按时间或大小封闭一个完整分段
                                     |
raw/{source}/{trading_day}/{segment_id}.arrow.tmp
                                     |
关闭写入器 -> 持久化文件 -> 校验结构、行数及哈希
                                     |
同文件系统原子重命名为 .arrow -> 登记已提交 manifest
                                     |
compact_ticks.py 读取已发布分段并生成新的规范版本
                                     |
canonical/tick/{exchange}/{instrument_id}/{trading_day}/{version}/part-*.parquet
```

- 时间窗、大小阈值、压缩和 Row Group 大小通过代表性样本测定；可合并同来源的多个合约以控制小文件数量。分段周期只是配置项，不能直接推导最大数据损失时间。
- 发布前的临时文件对读者不可见；崩溃后隔离未完成文件。完整文件已重命名但清单未提交时，恢复程序校验后补登记；清单只引用已完成且校验通过的文件。
- 使用目标平台的持久化接口，并测试进程崩溃、断电恢复及磁盘满等场景。分别记录内存队列、未封闭文件、已持久化文件和已提交清单的水位，按实测定义恢复点目标（RPO）。
- 每批行情保留本地采集序号、连接标识及来源序号（若有）。本地序号不能证明交易所到本地的链路从未丢包；断线和来源缺口需单独标记。
- 队列积压或写入失败必须计数并告警；超过配置的数据质量容忍度时禁止新增风险。录制故障不能静默丢弃数据，也不能阻塞订单回报处理队列。
- Compaction 由已封闭分段和交易时段水位驱动，可盘中或盘后执行，不统一绑定 15:30。重复执行不得重复纳入同一来源记录；排序、去重和修订规则均需版本化。
- 规范文件生成新版本后原子发布，保留原始分段及快照引用；按引用关系和保留策略回收旧文件，禁止覆盖仍被实验引用的内容。

若后续需要更低的行情 RPO，再引入有长度边界、序号、校验和、同步水位及残缺尾部恢复规则的追加日志，并通过故障注入验证。订单与成交从一开始就使用第 5.4 节的事务性交易事件库，不依赖行情分段成功才能恢复。

<a id="section-5-2"></a>

### 5.2 Bar、Tick 与结算数据 Schema

#### 1. 通用标识与时间

| 字段 | 类型及语义 |
| :--- | :--- |
| `instrument_id`、`symbol`、`exchange` | 稳定的实际合约标识、原始柜台代码、交易所；完整交割年月放在合约目录中，连续序列另有类型标识。 |
| `event_time` | 事件实际发生时间，统一存储为带 UTC 时区的时间戳，展示及交易日历计算使用 `Asia/Shanghai`。 |
| `receive_time` | 本地实际收到实时消息的时间；历史文件无此信息时为空，不能伪造为实测接收时间。 |
| `available_at` | 在回测中允许策略获知该记录的最早时间，来自历史发布时间或有明确标识的发布 / 传输延迟假设。 |
| `trading_day`、`session_id` | 结算交易日使用 DATE；时段标识用于跨夜盘、竞价、休市及特殊交易日处理。 |
| `source_id`、`source_seq`、`ingest_seq` | 来源及其序号（若提供）、本地采集序号；序号作用域及连接重置规则需要定义。 |
| `ingested_at`、`schema_version`、`quality_flags` | 本次导入时间、结构版本及质量标记；导入时间不等于历史可获知时间。 |

同一版本文件中不变的来源、Schema 等信息可放入文件元数据和 manifest，逻辑 Schema 必须完整可恢复。时间戳精度不能超过来源实际提供的精度。

#### 2. 分开定义业务数据

| 数据集 | 必需字段与处理规则 |
| :--- | :--- |
| Bar | `bar_start`、`bar_end`、`interval`、`open/high/low/close`、本周期 `volume` / `turnover`、期末 `open_interest`。明确区间边界、覆盖时段、Open 所属时刻、竞价归属及可见时间；未完成 Bar 不按最终 OHLC 发布。 |
| ExecutionReference（派生视图） | 实际合约、目标 `session_id`、`reference_time`、`price_type`、参考价格、源记录标识、来源版本与分辨率、可见时间及质量标记。仅从确实覆盖所选执行时点的原始数据生成，区分夜盘开盘、日盘开盘、下一 Bar 开盘及固定盘中时刻；成交量若来源未提供则为空，不借用整日成交量冒充该时刻可用量。 |
| Tick / L1 快照 | `last_price`、`bid_price_1/ask_price_1`、`bid_volume_1/ask_volume_1`、交易日累计成交量和成交额、持仓量、`pre_settlement_price`、实际 `upper_limit_price/lower_limit_price`、交易状态。更深盘口按来源能力扩展。 |
| Settlement | 实际合约、交易日、官方 `settlement_price`、前结算价、`published_at`、可见时间、临时 / 最终状态、修订版本及来源；结算修订作为新记录发布。 |
| Instruments | 原始代码及别名、完整交割年月、上市 / 到期及最后交易日、乘数、最小价格变动、账户最晚平仓限制、常规夜盘能力及其生效版本；关联短代码解析依据与历史合约目录。 |
| Sessions | 实际合约、`session_id`、`trading_day`、带时区的起止时刻、`phase`、报单 / 撤单 / 撮合权限、权限受理层级、竞价类型及规则引用、订单延续规则、该交易日是否实际有夜盘；关联日历版本、常规模板、例外公告、来源及适用柜台能力版本。 |
| InstrumentStatus | 实际合约、原始状态、规范交易阶段、来源及接收时间、有效范围与质量标记；保留与日历冲突的证据，不能用过期状态放行请求。 |
| DominantMapping / ContinuousFactors | 决策与生效时间、输入版本、映射合约、调整参考价与因子、收益率视图的两端合约和时刻；用于信号的所有输入满足当时可见性。 |
| AccountSnapshot | 账户、查询批次、所属交易日、结算版本、事件游标、一致性状态及资金 / 持仓视图；原始半结算结果与最后一致快照分开保存，不混入行情 Parquet。 |

Sessions 从已选规则版本生成实际时段，保存常规模板与节假日 / 合约例外的解析结果。起止区间按左闭右开解释，同一合约及时点的阶段与权限须唯一；正常休市显式表示，规则缺口或冲突标记为未知，不能静默补成连续交易。只撤不报窗口必须可独立表达；品种通常有夜盘不能替代该交易日的实际时段记录。

原始 Tick 的累计成交量转换成增量时，处理交易日重置、重复快照、连接中断和缺失首笔，不能简单对所有负差值取零。来源的单边 / 双边成交量口径、异常哨兵值、空盘口和时钟偏差均需规范化并保留原始值或可追溯原件。

规范目录按 `bar/1m`、`bar/5m`、`bar/1h`、`bar/1d`、`execution_reference`、`tick`、`settlement` 分层，再按实际合约、交易日或月份分区并保存版本。派生视图保留源数据及日历版本，不改变源记录的可见时间。文件路径使用安全编码后的标识。涨跌停控制优先使用对应交易日的实际价格边界；由比例推导时需按交易所舍入及特殊调整规则核验。

<a id="section-5-3"></a>

### 5.3 DuckDB 分析边界

- DuckDB 在本项目中负责已发布 Parquet 的只读分析，交易事件和盘中账户事务由独立存储处理。
- 查询从指定数据集 manifest 获取文件列表及版本，禁止把多个修订版本通过通配符一起读入，造成重复行情。
- 跨品种和基本面联表必须明确合约键、交易日、实际时刻及 `available_at` 条件；发布时间较晚的数据不能回填到较早的策略决策中。
- 在代表性数据上记录查询耗时、峰值内存及临时磁盘占用，设置资源预算，避免研究任务争用实盘资源。

<a id="section-5-4"></a>

### 5.4 SQLite、历史规则与费用模型

#### 1. 库的职责与耐久性

- `meta/control.db`：数据集 manifest、合约目录、交易日历、能力配置、历史规则来源和实验索引。
- `live/trading.db`：控制者及控制代次、订单意图及发送状态、柜台标识、待关联 / 唯一成交、规范事件、账本、预占、风控计数、生命周期任务、查询批次、处理游标及账户快照。控制代次的恢复须满足 4.5 的不回退要求，不能仅用旧业务快照覆盖当前控制记录。
- 每个库采用受控写入者和短事务，数据库位于本地受支持的磁盘，不用网络共享文件承担多进程交易协调。交易意图持久化失败时不得继续发送增加风险的订单。

连接初始化至少包含以下设置，并按实际 SQLite 版本检查生效结果：

```sql
PRAGMA journal_mode = WAL;
PRAGMA synchronous = FULL;
PRAGMA foreign_keys = ON;
PRAGMA busy_timeout = 5000;
```

`FULL` 用于保护已提交的订单及规则记录，但仍须在目标文件系统验证故障恢复。使用 SQLite 备份接口或一致性快照做备份，不能在活跃 WAL 模式下仅复制主数据库文件。Checkpoint、保留策略及恢复演练纳入运维。

#### 2. 历史规则结构

`contracts_rules_history` 关联历史合约参数、保证金规则及柜台能力；手续费独立存储开仓、平今、平昨的每手费用与金额费率。每条规则都包含生效区间、当时可获知时间、账户配置、来源及版本。

日历配套规则保存交易时段、竞价制度、夜盘及节假日覆盖，关联第二章的公告档案。规则按交易日生效时保留原始生效交易日，生成 Sessions 时结合品种夜盘映射解析适用的物理区间，不能按该自然日 00:00 简单切换。特定合约、最后交易日或节假日规则对常规模板的覆盖须显式声明，冲突未解决时不发布可交易日历；历史旧版本保持可重建。

下面为核心字段示例；合约、来源、保证金、能力和舍入规则使用配套目录表维护，实现迁移时补齐外键及版本区间校验。

```sql
CREATE TABLE contracts_rules_history (
    rule_version_id TEXT PRIMARY KEY NOT NULL,
    instrument_id TEXT NOT NULL,
    account_profile_id TEXT NOT NULL,
    effective_from TEXT NOT NULL,
    effective_to TEXT,
    available_at TEXT NOT NULL,
    multiplier TEXT NOT NULL,
    price_tick TEXT NOT NULL,
    margin_rule_id TEXT NOT NULL,
    capability_profile_id TEXT NOT NULL,
    source_id TEXT NOT NULL,
    CHECK (effective_to IS NULL OR effective_to > effective_from)
);

CREATE TABLE commission_rules_history (
    fee_version_id TEXT PRIMARY KEY NOT NULL,
    instrument_id TEXT NOT NULL,
    account_profile_id TEXT NOT NULL,
    offset TEXT NOT NULL CHECK (offset IN ('OPEN', 'CLOSE_TODAY', 'CLOSE_YESTERDAY')),
    effective_from TEXT NOT NULL,
    effective_to TEXT,
    available_at TEXT NOT NULL,
    fee_per_lot TEXT NOT NULL,
    fee_rate TEXT NOT NULL,
    rounding_rule_id TEXT NOT NULL,
    source_id TEXT NOT NULL,
    CHECK (effective_to IS NULL OR effective_to > effective_from)
);
```

- SQLite 示例中的精确数值使用规范十进制字符串并由领域层以 `Decimal` 解析；UTC 时间字符串固定格式和精度。校验拒绝非法数值、缺失来源及无法唯一确定的适用规则。
- 常规线性手续费公式为：`费用 = 手数 × 每手费用 + 成交价格 × 合约乘数 × 手数 × 金额费率`，再按对应规则舍入。纯按手或纯按金额时，另一分量为零；最低收费等特殊规则需显式扩展并测试。
- 规范费用表保存该账户最终适用的总费率。原始来源记录区分交易所费用、期货公司加收和期货公司已报总费率，避免再次叠加已包含的费用。
- 保证金配套规则描述按金额 / 按手、方向、投机 / 套保标志、组合分组及优惠资格；不能只存两个多空比例而丢失计算条件。
- 账户资金使用规则单独记录浮盈可计入比例、浮亏处理、汇总范围、冻结及费用口径，并关联账户配置、有效区间与来源；保证金比例表不能替代开仓资金权限。
- 生效区间按 `[effective_from, effective_to)` 解释，需支持日内调整。相同查询时点与账户下的适用规则必须唯一；历史修订保留旧版本，并规定替代关系和查询优先级。
- 成交记账按实际生效规则计费；策略和事前风控只能读取当时可获知的版本。晚发布的费用或结算更正按更正事件入账，不能提前提供给策略。历史发布时间不可考时，明确标记时间假设及其回测限制。

<a id="section-5-5"></a>

### 5.5 完整回测实验快照

每次回测生成不可变 `run_manifest.json`，至少包括：

- 数据文件列表、内容哈希、Schema、质量报告、下载及清洗版本。
- 策略代码提交或源码归档哈希，包含未提交改动；Python、依赖锁文件、运行平台及涉及的接口库版本。
- 策略类别、信号频率与参数、初始资金 / 持仓、账户资金使用规则、交易配对及盈亏归因规则、交易日历、手续费和保证金版本。
- 执行策略、目标时段 / 时刻、信号可见与订单生效规则、错过目标开盘后的处置；执行价格的数据源、版本、分辨率、来源字段与时段映射、缺失价格处理规则。
- 合约代码解析与目录版本、主力映射、连续收益构造及复权因子、移仓触发规则与腿序、撮合规则、`limit_liquidity_scenario`、成交量参与率、延迟分布、风险阈值及全部降级假设。
- 交易阶段及操作权限版本、解析后的 Sessions 文件或内容哈希、常规模板与例外公告引用、生效基准及来源核验状态；包含日盘再竞价 / 只撤窗口、实际夜盘、订单延续和竞价模型，不能重跑时自动换成最新日历。
- 生命周期与维护配置、未决请求的查询 / 告警超时、回放模式与倍速、故障注入配置、启用的 A25 子场景和证据类型；倍速只控制墙钟播放速度。
- 随机种子、同时间事件优先级、数据排序规则、时间范围、样本划分及输出哈希；清单不包含账户凭证。

引用的原始及规范文件必须按内容或版本不可变发布。硬链接只用于受不可变管理的源文件，禁止原地写入这些源文件；不满足条件时使用独立副本。目录只读标记本身不能保护共享文件内容。

同一环境、输入和随机种子下，重跑应得到相同订单、成交和账本结果；跨平台数值差异采用事先声明的容差并保留环境信息。数据补录、规则修订、代码或配置变更应生成新的实验标识，不能覆盖旧结果。

---

<a id="chapter-6"></a>

## 六、撮合模型、延迟与策略验证

<a id="section-6-1"></a>

### 6.1 Bar 撮合与信息可见性

使用完整 Bar T 的收盘信息生成订单后，最早只能在订单实际生效后的行情上撮合，通常为下一可交易 Bar。这约束的是时序因果与前视偏差，不能代替防过拟合的样本外验证。此前已经生效的订单可以在 Bar T 内成交，不能笼统禁止所有当前 Bar 成交。

#### 1. 订单生效与成交候选

| 订单场景 | 撮合条件与价格边界 |
| :--- | :--- |
| 主动成交意图 | 根据柜台能力转换为支持的订单类型，如带价格保护的限价单。仅在订单生效、时段及流动性条件满足时生成候选成交。 |
| 集合竞价、只撤窗口与小节休市 | 请求到达时按 4.2 核验报撤权限，撮合独立检查 `can_match()`；只撤窗口不接收新交易所委托且不撮合，撤单被拒绝不改变原单终态。集合竞价价格、数量及参与资格缺失时，不使用连续交易 Bar 冒充竞价撮合。 |
| 下一 Bar 开盘成交 | 必须在该开盘时点前已经生效，且满足竞价或连续交易规则；否则不能使用该 Bar 的开盘价。 |
| 限价买单 | `Low <= LimitPrice` 仅说明价格到达。开盘可成交时，候选价可由 `Open + 滑点` 得到，但不得高于限价或超出有效价格边界。 |
| 限价卖单 | `High >= LimitPrice` 仅说明价格到达。开盘可成交时，候选价可由 `Open - 滑点` 得到，但不得低于限价或超出有效价格边界。 |
| 盘中触及限价 | 配置“触价允许候选成交”及“穿价至少一个 Tick 才允许候选成交”等情景，结合成交量预算生成部分成交，不保证触价后全部成交。 |
| 涨跌停 | 使用候选成交时点适用的实际涨跌停边界与价格 / 交易状态，开盘候选不能因随后收盘触板而被否定。无更细盘口时按下述显式流动性情景评估；上市首日及连续单边市后的幅度调整使用当时实际边界并保留来源。对应验收为 A20。 |

启用竞价成交模型时，价格形成、并列价格处理、同价分配、有效期及未成交申报延续分别绑定适用交易所规则与可用数据；不能仅凭一所的“最大成交量”条款实现全市场竞价算法，也不能把开盘模型用于收盘竞价。只检查时段合法性不要求首版重建竞价盘口；不能支持竞价成交的策略按 4.2、A25-07 明确选择可验证的执行方式。

若订单在 Bar 内才生效而没有更细数据，保守模式延至下一完整 Bar 评估，避免使用可能发生在订单到达之前的最高价或最低价。无有效行情、零成交量、休市、停牌及价格边界不明时，按质量与订单规则处理，不能补造可成交价格。

开盘候选与 Bar 结束时的估算分开处理：开盘价格判断只使用该时点的信息；完整 Bar 的高低价、收盘状态及“一字板”分类，只能在 Bar 结束时用于评估尚未成交的订单，不能改写已确定的开盘成交。仅有 OHLCV 时，不推断触板与回落的盘中先后顺序。

`limit_liquidity_scenario` 写入运行清单，至少提供以下两种情景：

- **方向保守（默认研究假设）**：候选时点处于涨停价时假设买单不成交，卖单仅在已观测成交量、共享参与率预算、订单生效时点及价格约束均满足时允许候选成交；跌停反向处理。这是缺少盘口时的保守假设，不代表真实涨停买单绝不成交或卖单必然成交。
- **触板无成交（压力情景）**：对候选时点价格已处于涨跌停边界的新候选成交，两方向均假设不成交，并在报告中单列结果。一字板只表明该 Bar 内成交价格未变，并不能证明成交量为零或双向均无流动性；不得把压力假设写成市场事实，也不能根据后来的完整 Bar 分类撤销此前已确定的成交。

#### 2. 成交量、事件顺序与局限

- 定义 `participation_rate`，将本账户在同一 Bar 的可模拟成交量限制为规范成交量的一个比例；多个策略和订单共享预算，禁止每个订单各自使用整根 Bar 的全部成交量。使用整根 Bar 成交量是事后容量近似，不能证明开盘时可成交的数量；需要验证开盘容量时须使用对应时段的更细成交或盘口数据。
- 候选价格、可用数量、排队情景和订单有效期共同决定部分成交。成交价始终满足订单限价、价格步长和涨跌停约束。
- Bar 无法提供可靠的盘中价格路径。默认在该 Bar 处理完成时才向策略公开本 Bar 的模拟成交结果，禁止利用完整高低价或总成交量进行同 Bar 追单和重复换手。
- 同一 Bar 同时触及止盈、止损等互斥条件时，采用预先约定的保守路径或转用更细数据；禁止按最终收益选择有利顺序。
- 报告列出信号与执行数据分辨率、成交量预算、涨跌停流动性情景、触价规则和成交时刻近似。需要盘中连续反应的策略须升级数据与撮合模型。

#### 3. 日线与小时级信号的执行时点

信号频率不决定执行价格的数据粒度。执行时点按品种的版本化日历、信号 `available_at`、计算 / 报单延迟及订单生效规则确定，区分集合竞价与连续交易。常规情况下，部分有夜盘品种的日线信号可在当晚 21:00 执行，无夜盘品种可能在下一交易日日盘开盘执行；节前无夜盘、临时停市、特殊开盘时间及信号迟到均须重新选择实际可用时段，不能写死“自然日加一天”或统一 09:00 / 21:00。

| 执行策略 | 所需执行数据与约束 |
| :--- | :--- |
| 下一可交易时段开盘 | 明确归属目标夜盘 / 日盘时段的开盘或竞价价格。只有数据源定义证实日线 Open 正是所选时段、时刻的价格时，才允许使用它。 |
| 下一日盘开盘 | 独立的日盘开盘价或足以识别该开盘的盘中数据；包含夜盘的交易日日线 Open 通常不能提供日盘开盘价。 |
| 下一日盘收盘前固定时间 | 与所选时刻对齐的分钟或更细价格，按日历计算相对收盘的偏移；不能用当日日线 Close 代替，也不能用尚未完成的分钟 Bar 收盘价提前成交。 |
| 下一 Bar 开盘（适用于小时级等信号） | 对应实际合约的下一可用 Bar 开盘价及明确的时段边界；订单须在该开盘撮合前已生效。 |

上述策略均须先确定信号可见时间，再检查订单能否赶上目标撮合时点。错过目标开盘时，按预先配置顺延到下一符合条件的目标开盘或取消，不回填已过去的开盘成交；仅有开盘价不代表订单参与了集合竞价。所选执行价格缺失或分辨率不足时，明确禁用该执行策略或中止实验，不静默改用其他时段的价格，也不插值补造价格。配置、日历及执行数据来源写入 5.5 的运行清单，对应验收为 A21。

<a id="section-6-2"></a>

### 6.2 Tick 撮合、涨跌停与排队估算

CTP 常见行情属于间隔不固定的盘口快照，不能直接还原完整订单队列。数据源的 Tick 是快照还是逐笔记录，应写入运行清单。

- **委托合法性**：先检查目标柜台支持的订单类型、有效期、价格步长及该交易日涨跌停边界。不支持的报单拒绝或按已配置的转换规则处理。
- **涨跌停成交**：最新价触及涨跌停并不自动意味着委托无效；有效委托能否成交取决于方向、对手盘、排队及有效期。无对手流动性时保留未成交或按订单有效期结束。
- **主动成交**：按生效后可用的对手价量及保守成交量预算模拟；不能用过期盘口无条件填满订单。超出可见深度的数量应限制、分批或采用明确的冲击假设。
- **被动排队**：初始化估计的前方队列，区分该价位可能成交的数量、撤单与新增挂单。不能把全市场所有价位的累计成交量全部用于消耗我方队列。
- **部分成交**：估计队列耗尽后仍按剩余可成交量逐步成交，队列耗尽不意味着整笔委托立即全部成交。
- **不确定性**：队列倍数、成交方向推断、撤单分配和冲击参数以真实行情与成交观测校准，并输出多种情景；仿真对比用于逻辑验证，不能替代真实排队证据。L1 模型结果需标注为排队估算，不宣称交易所级精确重放。

<a id="section-6-3"></a>

### 6.3 基于时间的报单与撤单延迟

采用基于时间的事件调度，分别配置报单到达、撤单到达、订单回报、成交回报及行情传输延迟，使用毫秒或更细的真实时间单位。参数可为常量或固定随机种子的分布，来自实际观测或明确标注的压力情景。

- `cancel_arrival_at = cancel_sent_at + cancel_delay_ms` 只表示模拟请求到达。到达时须通过时段、订单及柜台权限检查，撤单被接受并完成处理后才生成 `cancel_effective_at`；被拒绝的撤单不生成生效事件，原单继续有效。实盘按已确认回报处理，不能用本地计时替代撤单确认，迟到成交仍进入共享订单状态机。
- 定时事件即使没有新 Tick 也应推进；不能用“两 Tick”表示固定网络延迟。500ms 间隔下两 Tick 约一秒，稀疏行情时则可能更长。
- 行情、订单到达与回报延迟独立建模，不让策略提前看到尚未到达的成交或盘口。相同时刻的成交与撤单按固定、可测试的顺序处理。
- 当行情分辨率粗于待模拟延迟时，使用保守的后续可观测行情或多情景结果，并标注分辨率限制；不能凭快照插值声称恢复了真实亚秒成交路径。

<a id="section-6-4"></a>

### 6.4 样本外验证与敏感性分析

1. 按时间划分训练、验证及保留样本；参数选择仅使用训练和验证数据，保留样本不参与反复调参。
2. 采用滚动训练 / 验证，所有因子、归一化和主力映射满足当时可见性。存在重叠标签或持仓窗口时，按其范围设置样本隔离与间隔。
3. 对手续费、滑点、成交量参与率、触价 / 穿价、延迟、换月时点和数据缺口做敏感性分析，保存试验数量与失败结果。
4. 报告至少包含扣费权益曲线、回撤、收益波动指标、换手、费用贡献、保证金峰值、未成交 / 拒单、未完成移仓、强平及数据降级信息。夏普、回撤、月度收益使用包含未平仓估值的权益序列，注明收益频率、年化因子、无风险假设和现金流处理；胜率、盈亏比及持仓时长使用固定配对规则下的扣费交易记录。另列分品种贡献、日盘 / 夜盘拆分、新旧合约价差、实际移仓成本、注明基准的展期归因及实际滑点对假设滑点，避免将价差重复计为收益。
5. 工程验收以账务、时序及故障恢复正确性为依据；策略评价另行记录样本外表现及其对成本和成交假设的敏感程度。国内 CTA 收益高度依赖行情周期，样本长度与品种数按第二章第 4 项的研究数据集要求执行，报告注明样本覆盖的市场周期。

<a id="section-6-5"></a>

### 6.5 影子模式与实际执行校准

仿真环境的撮合与排队机制须单独核验，其结果适合验证接口及交易流程，不能直接证明真实市场的成交假设。回测能否外推到实盘，以真实行情下的执行偏差为直接证据：

1. **影子模式**：接入真实 CTP 行情，策略与共享内核正常运行，执行端口接模拟撮合；记录每个订单意图的假设成交价、数量与时刻。
2. **小额并行**：通过首笔实盘门槛后，以最小手数并行运行，同一订单意图分别进入模拟撮合与唯一真实执行服务，关联比较成交价、成交数量、成交耗时和未成交比例。比较时对齐实际合约、方向、触发时点及手数，数量不同的结果单独列示。
3. **定期报告**：按周输出实际滑点对假设滑点、实际成交率对假设成交率、按品种和时段的分布及样本数量；偏差持续超出敏感性分析覆盖范围时，回测结论标记为不可外推，成本参数重新校准。报告区分实时观测、历史回放和故障注入，并列出尚未在真实市场观察到的条件。
4. **运行时长与场景覆盖分别验收**：扩大资金前，影子模式与最小手数并行实盘须连续运行至少一个完整自然月，交易时段与维护中断均留痕。换月、长假前后、涨跌停及断线等场景按 A18 另行覆盖；期间未自然发生的场景可用版本化历史回放或故障注入补齐，但不能折抵实时运行时长，不能计入实际滑点、成交率或容量证据。
5. **扩大范围**：A24 与上述时长 / 场景要求通过后，结合真实样本覆盖程度逐步增加资金。最小手数的成交证据只支持已观察的规模与条件，不能自动证明更大订单的冲击与容量；每次增量单独评估并持续校准。

<a id="section-6-6"></a>

### 6.6 历史行情驱动的全链路回放

`scripts/replay_market_data.py` 读取固定版本的历史 Bar / Tick，经行情端口、虚拟时钟、策略、共享内核、风控和模拟网关产生新的订单与成交回报；继续经过归一化、订单管理及账本，输出完整事件与差异报告。它复用回测引擎和既有领域逻辑，不另建一套交易系统。`replay_events.py` 则重放已有交易事实以验证恢复，两者的输入与验收目的分别记录。

- 回放强制绑定 `SimulatedGateway` 和隔离的状态存储，不加载实盘凭证或向真实交易端口发送请求；所需规则、结算数据与初始持仓一起锁定版本。
- 支持单步、原速、加速及尽快运行；交易日历、延迟、查询超时与定时任务均由虚拟时钟推进。倍速不改变事件时间、可见时间和同时间事件顺序，不以墙钟 `sleep` 决定业务结果。
- 可注入集合竞价边界、日盘再竞价 / 只撤窗口、夜盘残留单、历史规则切换、节假日停复市、休市、缺失行情、乱序回报、部分成交、断线与半结算查询。A25 子用例的历史原文引用、规则版本、输入事件及预期结果随回放固定保存；输入故障及随机种子明确，无新行情时仍推进结算、定时器和恢复任务。
- 固定输入在不同倍速下应得到相同的规范订单、成交、预占及账本结果；首次对照由独立手工样例确定，不能只检查两次运行彼此一致。
- 首版先交付 Bar 路径，Tick 路径随类别扩展；回放标记为离线验证，不代替目标柜台联调、实时运行时长或真实滑点证据。对应 A28。

---

<a id="chapter-7"></a>

## 七、工程目录规划

以下目录按实施阶段逐步创建，首版不要求一次性实现所有模块。交易领域逻辑集中在 `domain/`，适配器负责转换、I/O 及模拟执行。

```text
qh_trader/
├── 中国期货自动交易与回测工具构建规划.md
├── 中国期货交易时间段与集合竞价机制详解.md
├── 第二轮深度架构评审复核意见.md
├── pyproject.toml
├── uv.lock
├── .python-version
├── .gitignore                           # 排除凭证、运行数据库、行情和生成报告
├── config/
│   ├── settings.yaml.example            # 运行模式、路径和风险参数；不含真实凭证
│   ├── broker_profiles/                 # 柜台版本、能力映射及验证结果
│   └── rule_sources/                    # 公告原文索引、哈希、生效基准、版本与核验清单
├── qh_trader/
│   ├── core/
│   │   ├── constants.py
│   │   ├── objects.py                   # Bar、ExecutionReference、Tick、Settlement、Order、Trade 等
│   │   ├── event.py                     # 事件排序、账户执行序列与队列边界
│   │   ├── clock.py                     # 实时 / 虚拟时钟与定时事件
│   │   └── ports.py                     # 策略协议及执行、行情、时钟、日志、规则、映射、查询端口
│   ├── domain/
│   │   ├── orders.py                    # 订单状态机、父子单及成交去重
│   │   ├── positions.py                 # 今昨仓、逐单冻结和跨日处理
│   │   ├── ledger.py                    # 成交、费用、结算、资金流和更正
│   │   ├── smart_router.py              # 品种意图解析为实际合约，生成开平子订单计划
│   │   ├── rules.py                     # 生效规则、计费、保证金及能力查询
│   │   ├── limits.py                    # 交易所开仓 / 持仓限额、交割月资格等硬约束
│   │   ├── risk.py                      # 原子检查与预占、限流、熔断状态及节前钩子
│   │   ├── recovery.py                  # 恢复、查询合并和账户对账
│   │   ├── lifecycle.py                 # DailyLifecycleManager：每日任务、结算门禁与幂等
│   │   └── rollover.py                  # 消费已发布主力映射，执行账户移仓两腿状态机
│   ├── gateway/
│   │   ├── simulated_gateway.py         # Bar / Tick 撮合、涨跌停方向规则与延迟事件
│   │   ├── feedback_normalizer.py       # CTPFeedbackNormalizer：乱序、标识关联与待关联事件
│   │   ├── terminal_info.py             # 匹配版本的终端采集与上报适配，载荷不落日志
│   │   └── ctp_gateway.py               # CTP 连接、请求、查询与回调接入，不直接记账
│   ├── monitor/
│   │   ├── watchdog.py                  # 独立心跳监控、申请接管、对账与失联处置
│   │   └── control.py                   # 人工命令经当前控制权执行：暂停、撤单、减仓与恢复
│   ├── data/
│   │   ├── schemas.py                   # 时间、质量、合约标识及字段规范
│   │   ├── calendar.py                  # 版本化 Sessions、再竞价 / 只撤权限与公告覆盖
│   │   ├── contracts.py                 # ContractResolver：按历史日期与合约目录解析短代码
│   │   ├── execution_reference.py       # 按执行策略校验并派生价格，保留来源与时段
│   │   ├── downloader.py
│   │   ├── recorder.py                  # 有界队列、完整分段与写入水位
│   │   ├── storage.py                   # 原子发布、清单、校验及 Compaction
│   │   ├── dominant_contract.py         # 纯数据主力判定，发布当时可知且有生效时间的映射
│   │   ├── continuous.py                # 连续指标、复权及同合约收益率视图
│   │   ├── replay.py                    # 版本化历史行情源与可控回放驱动
│   │   ├── statement.py                 # 期货公司结算单解析与每日比对
│   │   └── snapshots.py                 # 完整实验清单及不可变数据引用
│   ├── infrastructure/
│   │   ├── journal.py                   # SQLite 事务、事件游标及一致性快照
│   │   ├── memory_journal.py            # 回测模式的内存事件适配器
│   │   └── observability.py             # 结构化日志、指标、告警与脱敏
│   ├── engine/
│   │   ├── base_engine.py               # 组织共享内核，不重复实现业务算法
│   │   ├── execution_service.py         # 唯一实盘交易出口，注入适配器并承载账户执行序列
│   │   ├── backtest_engine.py
│   │   ├── shadow_engine.py             # 真实行情加模拟撮合，关联真实成交比对
│   │   └── live_engine.py
│   ├── research/
│   │   ├── vector_backtest.py           # 向量化快速研究通道与参数扫描
│   │   └── cost_assumptions.py          # 研究模式默认成本假设
│   ├── strategy/
│   │   ├── base.py
│   │   └── examples/
│   └── analysis/
│       ├── performance.py               # 含分品种贡献、盈亏比、月度表、换月成本
│       ├── validation.py                # 时间划分、滚动验证与敏感性分析
│       ├── execution_quality.py         # 实际成交对假设成交的偏差统计
│       └── visualizer.py
├── tests/
│   ├── fixtures/                        # 小型可分发样本、手工账和预期事件
│   │   └── sessions/                    # A25-01—07：日历版本、来源索引及独立预期事件
│   ├── architecture/                    # 分层禁止依赖及循环引用检查
│   ├── unit/                            # 规则、路由、冻结和账本不变量
│   ├── integration/                     # 数据发布、回测及柜台适配
│   └── recovery/                        # 乱序、断线、崩溃和存储故障
├── data_storage/                        # 本地运行数据，不提交 Git
│   ├── raw/                             # 原始完整分段及来源原件
│   ├── canonical/                       # 分类型、合约、日期及版本的 Parquet
│   ├── manifests/                       # 已发布数据集清单
│   ├── snapshots/                       # 完整实验快照
│   ├── meta/                            # control.db
│   └── live/                            # trading.db 及一致性备份
├── runs/                                # 回测输出与差异报告，不提交 Git
└── scripts/
    ├── init_env.py
    ├── download_data.py
    ├── compact_ticks.py
    ├── validate_data.py
    ├── gaps.py
    ├── snapshot.py
    ├── scan_params.py
    ├── run_backtest.py
    ├── run_shadow.py
    ├── replay_events.py
    ├── replay_market_data.py
    ├── reconcile.py
    ├── parse_statement.py
    ├── run_execution_service.py
    ├── watchdog.py
    ├── control.py
    ├── preopen_check.py
    ├── backup_state.py
    ├── smoke.py
    ├── shadow_report.py
    └── report.py
```

---

<a id="chapter-8"></a>

## 八、运维、数据质量与可观测性

### 1. 运维工具

| 脚本 | 职责与输出 |
| :--- | :--- |
| `init_env.py` | 验证依赖、CTP 与采集组件版本、接入模式、模块依赖边界，迁移数据库，检查风险配置与规则来源，输出脱敏环境清单。 |
| `download_data.py` | 按实际合约下载样本，保存来源和字段覆盖信息；明确是否含结算价与历史修订。 |
| `compact_ticks.py` | 从已发布分段生成新版本 Parquet；校验记录计数与哈希后发布，重复运行不产生重复行情。 |
| `validate_data.py` | 验证 Schema、时间、合约、价格、成交量、盘口、结算、规则及所选执行时点的数据覆盖，输出质量报告及隔离记录；执行价格不足时明确失败。 |
| `gaps.py` | 根据品种日历识别预期交易时段，区分正常休市、无成交、断线和缺失，不按自然时间机械补齐。 |
| `snapshot.py` | 生成完整实验清单并验证引用数据的哈希、不可变性及可访问性。 |
| `scan_params.py` | 向量化快速研究通道：按当时可见映射拼接序列，共用执行时点与价格来源，用研究模式成本假设做参数扫描，输出候选参数集及敏感性摘要。 |
| `run_backtest.py` | 通过固定配置运行共享内核，保存订单、成交、账本、风险事件及报告所需指标。 |
| `run_shadow.py` | 真实 CTP 行情加模拟撮合运行策略，可选关联经唯一执行服务下单的最小手数实盘，记录假设成交与真实成交。 |
| `replay_events.py` | 从快照游标重放规范事件，支持固定故障情景，比较订单、预占及账本差异；保存输入版本及历史回放 / 故障注入标记。 |
| `replay_market_data.py` | 以历史 Bar / Tick 和虚拟时钟驱动策略到模拟回报的全链路；支持单步、倍速及故障注入，校验规范结果与手工样例；强制隔离真实交易端口。 |
| `reconcile.py` | 生成订单、成交、持仓和资金差异报告；修复通过共享恢复协调器执行并保留审计记录。 |
| `parse_statement.py` | 解析期货公司结算单，与本地账本逐项比对，输出差异并在超出误差时设置禁止新增风险状态。 |
| `run_execution_service.py` | 装配并启动 `engine/execution_service.py`，注入 CTP、查询与持久化适配器；恢复控制代次、账户状态与生命周期任务，串行完成控制权校验、对账及发送。 |
| `watchdog.py` | 独立监控策略与执行服务心跳；策略失联时申请接管，经旧命令隔离、代次切换及对账后再撤单 / 减仓；旧交易出口无法隔离时仅告警并保持禁止新增风险。 |
| `control.py` | 人工暂停、撤单、减仓、状态查询与恢复；影响交易状态的命令须通过当前控制者或正式接管，携带控制代次并经唯一执行服务执行。 |
| `preopen_check.py` | 按日历时段及配置提前量检查：唯一交易出口、控制代次、认证 / 采集结果、交易阶段权限、生命周期、结算完整性、账户快照一致性、行情时效、规则、监控及外部告警。 |
| `backup_state.py` | 创建 SQLite 一致性备份，记录游标与校验信息，支持离线恢复验证。 |
| `smoke.py` | 执行固定小样本，检查运行退出码及预期结果；持续运行和故障测试另行覆盖。 |
| `shadow_report.py` | 按周输出实际滑点对假设滑点、成交率、耗时、样本数及品种时段分布；区分实时、回放和注入证据，列出未实测条件与规模；超阈值时标记回测结论不可外推。 |
| `report.py` | 输出资金、成交、风险、数据质量、假设及样本外表现的 HTML 报告。 |

### 2. 数据质量与观测指标

- 校验字段完整性、合约代码解析及交易日映射、事件顺序、重复记录、可见时间、价格步长和边界、盘口有效性、累计量重置、交易阶段权限、结算覆盖及规则生效区间；区分正常小节休市与行情缺失。
- 缺失字段、无成交和无效值分别表达，不能将空盘口填成可交易的零价。价格有效域按实际合约、价差或复权序列类型判定；不静默修正异常数据后覆盖原件。
- 日志从基础阶段即启用，至少关联账户别名、策略、`controller_id` / `control_epoch`、`client_order_id`、柜台标识、`journal_seq`、交易日和规则版本；凭证及敏感终端信息脱敏。
- 指标覆盖行情年龄、队列积压、写入水位、未知请求数量及持续时间、待关联成交、对账差异、预占数量、阶段权限冲突、生命周期超时、资金可用口径差异、拒单原因、过期控制命令拒绝数、报撤预算、保证金比例及未完成减仓 / 移仓。
- 对断线、行情过期、账本差异、无法持久化、队列溢出和持续清仓失败设置明确的告警与风险状态动作，不静默吞异常。
- 使用代表性订阅合约数和报单负载测量资源、吞吐、尾部延迟和恢复耗时，记录测试硬件及数据规模，再确定容量预算。研究查询和 Compaction 避免挤占交易进程资源。

---

<a id="chapter-9"></a>

## 九、实施路线与验收标准

<a id="section-9-1"></a>

### 9.1 分步实施路线

依赖顺序为：**策略类别、执行数据与规则日历 → 共享订单、账本与风控 → 单品种简化 Bar 回测与快速研究通道 → 多品种、移仓与硬约束 → 仿真、恢复、唯一执行服务与看门狗 → 影子模式与最小手数实盘 → 扩大范围与精细撮合**。路线采用薄切片：阶段 3 先用简化撮合得到第一条可对账的权益曲线并开始策略研究，恢复、存储和精细撮合用例在后续阶段补齐；小仓位是安全边际，不是基本故障测试的替代。智能平仓、逐单冻结和账本在回测资金结果验收之前完成；日志和指标从基础阶段开始建设。

| 阶段 | 核心任务 | 可验收交付 |
| :--- | :--- | :--- |
| **阶段 0：策略类别、接口与数据核验** | 声明策略类别、信号频率、执行策略及价格数据粒度；核验首个合约、账户资金权限、交易小节、竞价与维护规则；验证 Python / CTP 及采集组件；取得工程样本与研究数据；整理目标范围的时段、竞价调整与停复市等历史公告及柜台能力证据。 | 类别及执行策略声明、环境与接入模式清单、数据覆盖清单、柜台能力表、带原文 / 生效基准 / 版本的规则登记、A25 子场景适用清单及跨日盈亏手工账；未核验细项登记来源缺口，不作为实盘默认。 |
| **阶段 1：数据契约、规则、日历与持久化（精简版）** | 建立历史合约目录及短代码解析、Schema、规则、Sessions 常规模板与公告例外、再竞价 / 只撤权限、实际夜盘与长假标记、ExecutionReference、SQLite 事务、不可变 Parquet 发布及日志；首版低频路线推迟 Tick 录制。 | A29 及已启用范围的 A25 日历 / 权限断言通过；规则适用时点唯一，历史旧版本可重建；日历、执行数据、计费与质量校验通过，规则冲突、代码歧义或执行缺价明确失败。 |
| **阶段 2：共享交易领域内核** | 实现订单与发送状态、待关联成交、品种路由、冻结、一套账本的两种盈亏视图、账户资金权限、原子风控、硬约束、生命周期及恢复协议；内存 Journal 和可控网关事件验证。 | A01、A07、A08、A02—A04 及 A26 的领域用例通过，持仓、冻结、费用及权益与手工账一致；分层禁止依赖与循环引用检查通过；未知订单不盲目重发。 |
| **阶段 3：单品种简化 Bar 回测与快速研究通道** | 连接虚拟时钟、时段权限、Bar 撮合、结算及生命周期事件、执行价格、快照和报告；交付向量化扫描与 Bar 全链路行情回放，接入 A25 的规则边界及残留单样例，开始策略研究。 | A05、A11、A15—A17、A19—A21、已启用范围的 A25 子用例及 A28 Bar 路径通过；固定样本及不同回放倍速结果一致，账务可对账，无未来信息改写决策或成交；产出扣费权益曲线及敏感性报告。 |
| **阶段 4：多品种、移仓与节前处理** | 覆盖第二章第 4 项的测试品种组合，补各新增品种及历史区间的 A25 子用例；分别交付数据层主力映射、连续指标 / 收益率视图与交易层移仓执行；实现可配腿序、分批移仓及长假钩子。 | A10 及新增范围的 A25 通过，未来交叉日不影响此前决策，合约切换不凭空产生动量或资金收益；移仓成本不重复记账，剩余仓位可追踪；多品种样本外报告产出。 |
| **阶段 5：CTP 仿真、恢复与看门狗联调** | 接入目标认可的仿真环境；交付唯一执行服务、终端采集适配、CTP 回报归一化与全交易命令控制代次校验；联调账户资金权限、现行时段与报撤能力、日常生命周期、恢复对账、结算单、接管、人工控制和备份。 | A02—A04、A06、A08 的资金口径、A09、A12 的订单合法性、A22、A23、A25—A27 的已启用当前能力经柜台联调与故障注入通过；A25 历史制度边界用原公告及固定版本回放验收，不能由当前仿真行为反推；A14 覆盖交易状态及已启用存储，Tick 故障待启用验收；A18 时长及场景证据通过；旧命令隔离及对账完成后才允许接管执行。 |
| **阶段 6：影子模式与最小手数实盘** | 完成期货公司接入及程序化交易报备；真实行情加模拟撮合运行；通过首笔实盘门槛后最小手数并行实盘；每日结算单对账；每周输出区分证据来源的执行偏差报告。 | A24 通过；影子模式与最小手数并行实盘连续运行至少一个完整自然月；换月、长假及涨跌停等按 A18 补齐场景证据，回放不折抵运行时长或充作实测成交；每日核对无未解释差异。 |
| **阶段 7：扩大范围与精细撮合** | 按真实执行证据逐步扩大品种、资金与账户范围；按新增策略类别启用 Tick 录制、撮合、全链路回放、延迟及排队估算，低频扩容不以启用 Tick 为前提。 | 启用时通过 A12 排队、A13、A14 Tick 存储及 A28 Tick 回放部分；每次扩展补充规则、适配及回归证据，重新核验实际滑点与容量。 |

GUI、复杂组合保证金、因子进化与低延迟优化根据实际策略需求安排，不占用首个可对账闭环的关键路径。

<a id="section-9-2"></a>

### 9.2 必须通过的验收案例

每个案例保存输入数据、规则版本、事件序列和独立计算的预期账本。数量及状态必须精确相等，金额按约定舍入单位比较；预期结果不得直接由待测算法生成。柜台集成用例同时保存脱敏后的查询或结算依据。

| 编号 | 场景 | 通过条件 |
| :--- | :--- | :--- |
| A01 | 平今、平昨、统一平仓及接口能力 | 子订单映射符合选定柜台能力；合并 / 拆分后持仓与费用归属正确，不支持的类型明确拒绝。 |
| A02 | 成交先于报单回报、占位关联、部分成交后撤单及重复 / 乱序回报 | 真实成交不丢失且只记一次；迟到报单只补关联，不重复记账或预占；跨会话 OrderRef 冲突不误绑定；终态仍接收迟到成交，冻结不变量成立。 |
| A03 | 明确未发送、本地返回 0 后断线、发送超时及空查询 | 三态发送模型与订单状态分别正确；仅明确未发送或远端已决事实释放对应预占；30 秒等配置超时和查无此单只升级核对 / 告警，不自动重发原意图。 |
| A04 | 重启、跨会话撤单、查询限流 / 期间成交及外部委托 | 游标重放和查询合并不重复记账；撤单使用实测支持的原订单标识，不拼接新会话与旧 OrderRef；限流或未完成查询不被视为完整快照；能识别外部订单及差异，一致前不进入 `READY`。 |
| A05 | 周五夜盘、周六凌晨、节假日前后、无夜盘品种及特殊收盘时段 | 交易日、Bar 边界、计数重置与今昨仓转换均符合样例日历；停复市按公告和实际时段处理，不按自然日加减或统一 15:00 处理，并与 A25-05 的节后竞价一致。 |
| A06 | 结算价迟到、非报价 Tick 的结算价、重复结算及修订 | 待结算状态明确；结算价保留所需精度；每个版本只生效一次，更正留痕，已结转盈亏不再次增加权益；跨日结果符合手工账。 |
| A07 | 按手、按金额、组合收费、平今平昨差异、券商加收及日内变更 | 费用与独立手工账一致。单位样例：2 手、价格 1000、乘数 10、每手 1.5 元、费率 0.0001 时合计 5 元；这些是测试输入，不是实际品种费率。 |
| A08 | 并发开仓、活动占款、浮盈使用权限、浮亏及保证金优惠变化 | 检查与预占原子执行；允许 / 不允许浮盈开仓的配置分别符合预期，结算转入余额后按新日规则处理；开仓与保证金覆盖视图不相加，冻结不重复扣减；平仓后的资金需求正确重算。 |
| A09 | 熔断时仍有挂单、内部冷却、柜台限流或无法平仓 | 合法减仓不会被普通开仓冷却误拦；仍遵守硬限制；未成交风险持续可见，不将清仓请求当作清仓成功。 |
| A10 | 主力切换价差、交叉日时序、两腿部分成交及资金不足 | 仅未来交叉日不同的成对样本，既往决策一致；新旧合约各自价格不变而存在价差时，切换不产生虚假动量或账本盈利；主力映射不直接改变持仓，先开后平满足资金与敞口限制；费用逐笔计一次，失败报告真实剩余仓位。 |
| A11 | Bar 收盘信号、订单盘中生效、仅触价、零成交量及多单竞争 | 信号不回填到此前价格成交；成交量预算共享，部分成交及限价边界正确，不读取未来 Bar 或未知盘中路径。 |
| A12 | 涨跌停、无对手盘、不支持的市价类型及队列估算 | 委托合法性与成交条件分别判断；无流动性不虚构成交，排队与部分成交遵守所选情景。 |
| A13 | 撤单到达 / 生效、拒绝撤单、稀疏行情及延迟回报 | 定时器独立于 Tick 数推进；到达不等于生效，被拒绝的撤单不终结原单；有效撤单前成交正常入账，生效后不再产生新成交，迟到回报仍处理。 |
| A14 | 文件未封闭、重命名后清单未提交、磁盘满及交易日志写入失败 | 已发布数据可校验，孤立完整文件可恢复登记，残缺文件隔离；无虚假的持久化成功记录，交易意图未落盘时不继续增加风险。 |
| A15 | 同实验重复运行、规则修订、源文件新版本及硬链接快照 | 固定环境、虚拟时钟和编号规则下，订单 / 成交 / 账本规范化哈希一致；发布新版本不改变旧快照内容。 |
| A16 | 重复快照、累计量重置、休市、缺口、无效盘口及晚发布数据 | 质量问题可追溯，正常休市不误报；不通过补零或提前可见填补错误，精确模式对关键缺项明确失败。 |
| A17 | 参数选择、滚动验证及成本压力情景 | 保留样本未参与调参，拟合数据不越过时间边界；试验、失败结果和敏感性报告完整保存。 |
| A18 | 连续仿真运行与夜盘、结算、换月、长假、涨跌停、断线及备份恢复覆盖 | 实时连续仿真至少一个完整自然月，交易时段运行及维护中断留痕；所选品种夜盘、每日对账、换月、长假前后、涨跌停、计划断线和备份恢复均有验证证据。未自然发生的场景可用版本化历史回放或故障注入补齐，不能替代运行时长；无未解释的订单、持仓或资金差异。 |
| A19 | 交易所硬约束：日内开仓限额、持仓限额、交割月资格及涨跌停幅度调整 | 回测与实盘使用同一实现；超限意图被拒绝或提前平仓，违规在回测报告中可见；限额按合约阶段与生效日期正确切换。 |
| A20 | 开盘与收盘触板时序、涨跌停方向假设及有成交量的一字板 | 固定开盘输入及数量预算，仅改变后来收盘是否触板，既有开盘成交必须不变；一字板有量样例按方向保守假设及共享预算评估，双向无成交只作为单列压力情景；零量不成交，不推断未知盘中路径；上市首日与单边市幅度使用当时实际边界。 |
| A21 | 执行策略、日夜盘价差、节前无夜盘、特殊时段及数据不足 | 日夜盘开盘价不同的样例按配置使用正确来源与交易日，含夜盘日线 Open 不冒充日盘 Open；节假日及特殊开盘遵循日历，信号 / 订单迟到不回填过去开盘；固定盘中时刻缺价或分辨率不足时明确失败，不静默回退；清单能追溯执行数据版本。 |
| A22 | 结算单解析、两种盈亏视图与每日对账 | 资金、持仓、盯市 / 逐笔平仓盈亏及费用与各自正确口径一致；4.4 无费用样例的 +100、−20 与逐笔 +80 对齐，权益只增加 80；部分平仓及费用分摊可追溯，差异触发禁止新增风险。 |
| A23 | 策略终止、失联后旧进程恢复、人工接管竞争及执行服务故障 | 超时被检测；唯一执行出口先停发旧命令、持久化新代次并对账，再允许新控制者操作；旧代次开仓、平仓、撤单、改单及恢复命令均被拒绝，已发送请求的迟到成交仍正确入账；重启 / 备份恢复不复用旧代次，无法隔离旧出口时不另建连接自动交易；过程可审计。 |
| A24 | 影子模式执行偏差与证据范围 | 同一意图的模拟与真实成交按合约、方向、时点及手数正确关联；周报输出滑点、成交率、耗时分布与样本数，回放 / 注入结果不计入实测统计；未实测条件与规模单列，最小手数结果不自动证明更大容量；偏差超出敏感性范围时标记回测结论不可外推。 |
| A25 | 日盘再竞价 / 只撤窗口、历史版本、夜盘残留单、小节休市、节后恢复及撤单边界 | 按 [A25 子用例](#acceptance-a25)逐项验收已启用的品种与历史范围；报、撤、撮合权限独立且版本唯一；跨截止撤单被拒仍保留原单 / 预占，时段切换不重复建单或记账；不凭连续交易 Open 假定参与竞价。 |
| A26 | 每日生命周期、半结算查询、维护延迟及日终重启 | 持仓已切换而资金未结转的快照不覆盖最后一致状态；禁止新增风险但保持回报、对账及风险观测；登录成功不自动恢复交易；重复日终任务不重复结算、转换持仓或清零计数，未就绪越过开盘仍保持门禁。 |
| A27 | 终端采集组件、接入模式、认证与信息上报 | 匹配目标平台 / API 版本，按直连或中继模式核验调用顺序；缺库、载荷长度错误、采集 / 认证失败不能进入可交易状态；`ReqAuthenticate` 不被当作采集载荷上报接口；日志与快照不包含原始终端标识或采集载荷。 |
| A28 | 历史行情驱动全链路、倍速与故障注入 | 从行情输入实际产生策略意图、风控结果、模拟回报及账本；单步、原速、加速结果与独立预期一致，空行情时段定时器仍运行；真实交易端口不可启用，回放证据不混入实盘统计。Bar 先验收，Tick 随能力启用。 |
| A29 | 郑商所短年份、跨年 / 跨十年及代码歧义 | `TA405` 在不同历史日期依合约目录解析到各自完整交割年月，结果不随程序运行年份改变；挂牌 / 到期区间正确约束候选，缺日期或多候选明确失败，原始代码与解析版本可追溯。 |

A18 的运行时长是本项目验收目标，不代表交易所认证标准，也不能代替故障用例。场景证据逐项标记为“实时观测 / 历史回放 / 故障注入”，保存事件时间、输入数据、日历及规则版本、回放或注入配置与结果；已用回放覆盖但未在真实市场观察的场景仍须在校准报告中列明。回放 / 注入不能折抵实时运行时长，也不能证明实际滑点、成交率或容量。A14 按已启用存储验收，启用 Tick 录制前须补齐对应文件故障用例。每项未通过的原因、影响范围和禁用能力记录在验收报告中。

<a id="acceptance-a25"></a>

#### A25 交易时段与集合竞价边界子用例

子用例按已启用的交易所、品种、历史区间及执行能力形成适用清单。中金所、期权或不在本次研究范围内的制度版本可以标记为未启用；已使用范围的规则或数据缺失须标记为待核验 / 失败，不能以未启用规避。每项保存公告来源、规则版本、解析后的 Sessions、输入行情与订单事件、独立预期权限及账本；历史回放、注入与当前柜台实测分别标记。基础时段权限可用 Bar 路径和可控网关事件验证，不以精细 Tick 排队为前提。

| 子编号 | 输入与边界 | 通过条件 |
| :--- | :--- | :--- |
| A25-01 | 螺纹钢 / 豆粕 / 甲醇在 10:20、13:10；启用中金所时增加 IF 同时点对照。 | 商品在小休、午休均不生成成交，IF 按其连续交易规则处理；报单、撤单及柜台预埋是否受理按独立能力配置判断，不能由“不撮合”直接推导。 |
| A25-02 | 正常交易日 rb、m、MA 在 08:56；包含仍有效的夜盘部分成交残留单和新报单，按启用品种分别取样。 | rb / m 在适用版本下进入日盘竞价，MA 只撤不报且不撮合；残留单保留原标识、累计成交及剩余预占，时段切换不重复建单、预占或计费。 |
| A25-03 | 三所日盘竞价调整前后的历史样本，按原公告核验时间底稿记载的 2023-05-26 生效边界；另测生效交易日与前一自然日夜盘的关系。 | 事件命中已核验旧、新规则及正确交易日；不能把新竞价制度回填旧历史，也不能按自然日 00:00 错切版本；重复回放得到相同阶段与订单结果。 |
| A25-04 | 撤单 08:58:58 发出、08:59:01 到达；另测恰在 08:59:00 到达，及原单随后的竞价成交回报。 | 按左闭右开边界进入不可撤阶段时，撤单被拒绝，原单及对应预占继续存在，成交只入账一次；夜盘和中金所按各自截止时点复用边界验证。 |
| A25-05 | 明确节假日公告样例，覆盖周末连长假、节前取消夜盘和节后首日；包含 CZCE 常规有夜盘品种。 | 不产生公告取消的夜盘，不按“假日前一自然日”错关时段；公告要求的节后日盘竞价覆盖常规只撤模板；交易日、Bar、今昨仓及执行时点与 A05、A21 一致。 |
| A25-06 | 启用中金所时，使用当时已挂牌国债合约验证开盘时间调整前后（时间底稿记载 2020-07-20）及最后交易日。 | 竞价及开盘时间命中适用历史细则，不套商品时段；最后交易日下午不存在可撮合时段，不能由常规 15:15 收盘模板补出成交。 |
| A25-07 | 仅有连续交易 Bar Open，缺竞价价格、数量或订单参与资格；含目标竞价已经结束后才生效的订单。 | 明确拒绝竞价成交假设，或按事前声明改用连续交易后的可观测执行；不回填已经过去的开盘成交，缺少替代执行数据时明确失败；清单保存原执行意图、降级规则及实际数据来源。 |

阶段 1 验证规则解析与权限，阶段 3 经 A28 Bar 回放验证时序、订单及账本，阶段 4 补新增品种和历史范围，阶段 5 联调当前柜台能力。A25-03、A25-06 的历史制度不能靠当前柜台证明；历史原文与固定版本回放缺一不可。A25 通过也不自动证明竞价成交率、真实滑点或容量，相关证据继续按 A24 核验。

<a id="section-9-3"></a>

### 9.3 交付与上线条件

- **研究交付**：固定样本、两种盈亏口径的手工账、实验快照、Bar 全链路回放与可复现命令，以及成本、风控、数据质量和多品种样本外报告；合约解析、连续收益、适用历史时段及例外公告可追溯，包含已启用 A25 子用例与解析后的 Sessions。
- **模拟盘交付**：已核验的柜台时段 / 资金 / 终端接入能力、分阶段报撤与预埋语义、脱敏联调证据、分别记录时长与场景覆盖的运行报告、生命周期与半结算演练、唯一执行服务及接管记录、备份、开盘检查和人工应急手册；历史制度回放与当前柜台证据分别登记。
- **首笔实盘门槛**：前置阶段已启用能力验收通过，至少完成 A02—A04、A08 的账户资金口径、A14 的交易状态持久化、A22、A23、A25—A27；程序化交易报告与接入手续按适用制度及期货公司要求完成。报撤计数是审计材料，高频认定按有效规则与账户范围核验。A12 的订单合法性、A25 的时段 / 撤单边界适用于所有策略，启用范围内全部适用子用例通过且来源可追溯，现行权限须有目标柜台证据；Bar 路径须通过 A20、A21、A28 的 Bar 部分，历史合约加载须通过 A29。盘口排队、Tick 回放与 A13 精细延迟仍按类别启用，不作为低频策略的实盘前置。
- **扩大资金门槛**：A24 通过，影子模式与最小手数并行实盘连续运行至少一个完整自然月；按 A18 完成场景覆盖并区分证据来源，未自然出现的场景可回放 / 注入验证。实际滑点与成交率落在敏感性分析覆盖范围内，列明尚未实测的条件；增量资金分步验证冲击与容量，不能用最小手数样本直接外推。
- **生产就绪**：接入、规则、资金权限、风险和运行版本确定；唯一交易出口、控制权及全部交易命令代次校验可用；生命周期、结算完整性、交易阶段与账户快照一致，账户无未决订单 / 成交或未解释差异；数据、持久化、监控、外部告警和人工处置可用。
- **范围扩展**：每增加交易所、品种、历史研究区间、账户、订单类型、保证金模式、策略类别或数据源，补充相应规则来源、适配验证及回归案例，并更新 A25 适用清单；既有样例通过不能自动证明新增范围正确。中金所期权收盘竞价及未来新增夜盘按扩展范围单独验收。
