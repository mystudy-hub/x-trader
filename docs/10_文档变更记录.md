# 10 文档变更记录

| 项目 | 内容 |
| :--- | :--- |
| 版本 | 0.2 |
| 日期 | 2026-09-15 |
| 状态 | 文档一致性校验通过；实现与柜台联调未执行 |
| 依据 | [文档拆分独立复审意见.md](reviews/文档拆分独立复审意见.md)；用户指示“按照你的建议进行修改”；目录整理依据见文末 |
| 上游 | 来源规划、00—09、独立复审报告 |
| 校验入口 | python scripts/check_docs.py --check |

<a id="v0-2"></a>

## 0.2 修订范围与解释顺序

原始规划保留为历史来源，SHA-256 为 `C802C02F6F2768B354351EF0A0E8CA9925A274F2BB9DFCFEEAE54D8115452D34`。本轮恢复遗漏、修正跨文档冲突并统一维护方式；A01—A29、A25-01—07 的场景和通过条件、S0—S7 的核心任务与可验收交付、21 个脚本职责、五级交付/上线条件继续以原规划为依据。

当前已确认约束由文档基线及明确的变更记录解释；无已确认变更覆盖的部分继续遵守原规划。PROP-01—09 仍为待确认议题，本轮统一提案表述与引用，不采纳新的资金、时长、交易能力或技术选型门槛。

## 复审问题落实

| 复审项 | 本轮修改 | 对应文档 / 要求 |
| :--- | :--- | :--- |
| 1 优先级后移 | 改为按阶段与适用范围生成优先级；基础时段、自成交、向量化保留既定阶段，精细撮合按启用范围；修正 FR-CON-04/05 的引用 | 02、03 §6、06；requirements.json |
| 2 命令与事实混流 | 同一账户执行序列分成交易命令与事实处理分支；旧代次、熔断和当前资金不足不否定真实成交；明确事务提交失败后的门禁与恢复 | 02 FR-RISK-01 / FR-REC-03；04 §6.1；05 §16 |
| 3 领域层直读 SQLite | RuleStorePort 的 SQLite 读取/迁移归基础设施适配器，领域规则消费注入的端口和值对象；规则查询区分 effective_at 与 known_at | 04 §4.5；05 §22；06 S1-05 |
| 4 价格有效域遗漏 | 补回按实际合约、价差、复权序列类型判定有效域；加入 A16 固定规格夹具 | 02 FR-DATA-08；04 §7.6；07 §4.5；08 §4.1 |
| 5 运维门禁歧义 | 监控/外部告警不可用时禁止新增风险；行情质量容忍度与交易事实可靠保存失败分开；补 F19、F20 | 07 §7；08 §2.1、§4.2 |
| 6 追溯未闭合 | 用单一清单生成 R 表、需求元信息、双向矩阵、阶段/优先级和验收关联；补 R-106—108，区分 S5/S6 时长归属 | 01 §10；02 §4—7；03；06；07；check_docs.py |
| 7 未标记新增限制 | 允许经授权、版本固定的研究数据作为工程补充样例；允许实验清单附文档/需求版本审计元数据 | 07 §4.4；06 §9；00 维护规则 |
| 8 术语和待确认议题 | 仅冻结字段由预占汇总；S0—S7 为八阶段；core 层内允许无环依赖；修正恢复引用；统一 S3a/S3b、自然月加场景及数据采购提案 | 01 术语；06；08；00 PROP 索引；02/03/09 |

另将临时就绪门禁的自动重评与既有熔断/人工暂停的显式恢复区分，避免日常结算或行情恢复自动解除人工暂停。时钟提案明确：单调时钟测耗时；事件时间与接收时间差含传输/处理延迟，不直接认作纯时钟偏差。

## 编号与阶段口径

- 既有 R、FR、NFR、A、D、ADR、X、O 编号保持稳定。
- R-106、R-107 补齐数据质量和向量化通道追溯；R-108 与新增正式 FR-VAL-10 对应原规划已要求的 S5 连续仿真，不新增运行时长门槛。
- 原先未入索引的研究数据 S0 硬出口建议登记为 PROP-09 / FR-DATA-11 / D-13，仍待确认。
- 当前登记 108 条 R、97 条正式 FR、7 条 NFR、9 条待确认 FR；正式需求共 104 条。
- M3 是单品种工程闭环，M4 是完整多品种研究交付，M6 是影子与最小手数实盘校准；已交付基础约束继续适用于后续阶段。
- 历史 Bar 发布属于 S1 必需；Tick 录制/Compaction 默认随 S7 能力启用，若提前启用则提前完成相关验收。账户已启用的保证金规则和追保/强平要求属于对应阶段必需，新增复杂组合优化继续按范围启用。

## 维护与校验

`docs/requirements.json` 是需求元信息及关联的唯一维护入口。业务正文仍人工维护；带 GENERATED 标记的表格及 02 需求元信息由 `scripts/check_docs.py` 生成。

```text
python scripts/check_docs.py --write
python scripts/check_docs.py --check
```

校验覆盖：编号唯一、来源锚点、正式需求的 R 关联、验收孤立项、待确认议题隔离、阶段与优先级、派生表一致性，以及原始案例、阶段任务、脚本职责、五级门槛、SQL 和目录路径保留。

`tests/fixtures/` 提供 4 份 JSON、15 个固定规格场景，覆盖两种盈亏、手续费、订单回报、撤单边界与价格类型校验。它们均为未执行规格，不属于已通过的 A 系列测试，也不替代真实公告和柜台证据。

本轮已执行生成与只读校验，结果为：108 条 R、104 条正式需求、9 条待确认需求、202 条验收关联、4 份规格夹具通过结构与一致性检查。36 条原始验收行、8 个阶段的任务/交付、21 个脚本职责、5 级门槛、SQL 及原目录路径保留；来源规划哈希一致。

另以仅在内存中修改的数据验证校验器：缺失 R 关联、待确认项进入正式交付、未知验收编号、重复 R 编号、篡改原始通过条件这五种反例均被拒绝；派生表漂移可被检测并按清单恢复。跨日盈亏与手续费夹具的独立算式核对通过。

这些结果仅证明文档、追溯与规格数据的检查已完成，未执行交易内核 A 系列测试或柜台联调；夹具状态仍为 not_executed。

<a id="documentation-layout"></a>

## 2026-09-13 文档目录与导航整理

依据：根目录文档整理建议及用户选择第 1 项、随后要求继续整理。文档基线保持 0.2，本次维护项目入口、目录分工与追溯路径。

根目录新增 [README](../README.md)，汇总项目状态、文档入口和维护命令；[00 文档索引](00_文档索引.md)补充按目的阅读路线、文档职责与解释顺序；04 的工程目录规划同步下列迁移。

| 原根目录文件 | 当前正文位置 |
| :--- | :--- |
| 中国期货自动交易与回测工具构建规划.md | [docs/sources/中国期货自动交易与回测工具构建规划.md](sources/中国期货自动交易与回测工具构建规划.md) |
| 中国期货交易时间段与集合竞价机制详解.md | [docs/references/中国期货交易时间段与集合竞价机制详解.md](references/中国期货交易时间段与集合竞价机制详解.md) |
| 第二轮深度架构评审复核意见.md | [docs/reviews/第二轮深度架构评审复核意见.md](reviews/第二轮深度架构评审复核意见.md) |
| 文档拆分独立复审意见.md | [docs/reviews/文档拆分独立复审意见.md](reviews/文档拆分独立复审意见.md) |

来源规划按字节保留，SHA-256 继续使用本记录中的冻结值。其正文中的两个相对链接由 sources/ 内的简短跳转页连接到对应正文。评审报告的链接改用仓库内相对路径，来源规划的旧行号链接改为对应的稳定章节锚点，历史审查结论保留。

`requirements.json` 的来源文件路径及 4 份规格夹具的 `source_refs` 同步到新位置；路径均相对于仓库根目录。`baseline.document_moves` 登记上述迁移，校验器按该映射核对原工程目录中的 Markdown 路径，并继续检查其余规划路径完整性；映射仅允许将同名根目录 Markdown 文件迁入 docs/。规格夹具校验增加来源路径和锚点检查。

通过 `.gitattributes` 对冻结规划设置 `-text`，禁用 Git 的自动换行转换，保证后续检出时用于 SHA-256 的字节一致。

验证记录：`python scripts/check_docs.py --write` 与 `--check` 通过，派生表无需重写；19 份 Markdown 中的 149 处本地链接、50 个锚点通过检查。冻结规划与原 Git 文件逐字节一致，参考底稿哈希保持不变；需求清单及规格夹具与迁移前数据比较，仅来源路径与迁移元信息变化；两份评审报告仅修改链接目标。另以 5 个内存反例验证：缺失原交易模块、缺失迁移后的文档路径、将代码模块伪装为文档迁移、夹具仍引用旧路径及引用不存在的锚点，均被拒绝。

## 2026-09-13 Day 0 自动检查入口

依据：用户选择补齐 CI，落实 06 §10 的 D0-6。新增 [scripts/check_ci.py](../scripts/check_ci.py)，使用当前解释器在仓库根目录运行架构测试、单元测试、smoke 及只读文档校验；汇总子检查失败并返回非零退出码。

新增 [GitHub Actions 工作流](../.github/workflows/ci.yml)，在推送、PR 更新和手动触发时使用 Windows runner。uv 固定为本地已使用的 0.8.4，依照 `.python-version` 选择 Python，并通过 `uv sync --locked --group dev` 安装依赖。README、00 与 04 同步检查入口及目录说明；测试覆盖整体通过，以及四项子检查分别失败时的退出状态和完整执行行为。

CI 状态以实际运行记录为准；本次工作流配置需随提交推送到 GitHub 后触发托管检查。S0 核验及交易功能验收继续按各自证据记录。

本地已通过统一检查入口：6 项架构测试、36 项单元测试、smoke 和文档校验全部通过；单元测试包含四项子检查分别失败时整体返回非零状态的回归用例。新增 Python 文件的 Ruff 检查、工作流 YAML 结构及更新文档的本地链接检查通过。GitHub 托管运行尚未执行。

## 2026-09-13 Day 0 全项整改与验收

依据：用户要求完成 Day 0 全部要求。按既有 NFR-03 补齐 core、domain、strategy、四类适配器和 engine 的允许依赖矩阵；engine 本层无环引用属于内部实现组织，跨层范围仍为 core/domain。静态依赖图纳入 scripts，并检查包根导出形成的传递依赖及全局循环；research/analysis 保留原文未单列白名单的范围，同时受入口单向性和全局无环约束。

配置模板按 FR-SCOPE-02/03 区分能力类别与策略风格，执行策略对齐 FR-EXEC-02；新增显式核算模式与错过时点处置。移除未核验柜台端点和撤单限额默认值，保留 S0/S2 待确认项；交易库与规则库分别指向 `data_storage/live/trading.db` 和 `data_storage/meta/control.db`。smoke 解析并验证模板，Git 忽略规则补齐数据库旁路文件及终端载荷。

README 明确 main 主分支和提交摘要规范。本地 D0-6 使用版本化 `.githooks/pre-commit`，导出暂存区快照后运行统一检查；工作区未暂存内容保持原样。快照按自身忽略规则阻断被强制暂存的运行或凭证文件；安装器保留已有自定义 hooks。GitHub 工作流继续作为托管检查配置。

09 中 Python/CTP 与凭证加载的状态改为“进行中”，分别保留已验证的工程子项；撤回 A25-01/A25-07 与现有夹具不匹配的“已核验”标记。规则来源规范补齐日内生效与可获知时刻，柜台能力规范补齐适用范围和逐项核验状态。上述调整恢复既有约束和证据口径，文档基线仍为 0.2，交易验收及柜台联调状态不变。

逐项验收状态见 [06 Day 0 验收记录](06_开发计划.md#day0-acceptance)。

本轮验证：8 项架构测试、177 项单元测试、smoke 和文档校验通过；其中 8 项独立 Git 仓库实测覆盖提交触发、暂存失败不能被未暂存修复掩盖、未暂存内容保留、强制暂存忽略文件被阻断，以及已有 hooks 被保留。修改的 Python 文件通过 Ruff，8 个运行依赖版本与锁文件一致，49 个规划业务模块与文档链接检查通过，来源规划哈希保持一致。

当前仓库已执行 `scripts/install_hooks.py`，`core.hooksPath` 指向 `.githooks`，D0-6 按本地自动检查路径完成。Day 0 八项交付验收通过；GitHub 托管运行仍待推送后记录，S0 尚未验收。

## 2026-09-13 S0 复审：恢复规格夹具基线

依据：用户选择修复 S0 复审第 1 项。`ledger_examples.json`、`order_event_examples.json`、`session_boundary_examples.json` 已按提交 `28f2e1f` 逐字节恢复，保留原有 schema、夹具标识、来源锚点、验收关联、独立输入/预期及 `not_executed` 状态。四份正式规格夹具恢复为 15 个场景；扩展场景须按现有结构另行核验后添加。

本次改写稿已保存到本地 `runs/review_backups/20260913T154201999016Z_s0_fixtures/`，附原稿和恢复版本的 SHA-256 清单。该目录由 Git 忽略，备份内容不作为验收预期。S0 出口检查及总结文件的其余复审问题仍待修正，不能据本次恢复认定 S0 已通过。

恢复后运行统一检查入口：8 项架构测试、177 项单元测试、smoke 与文档校验全部通过；三份文件与 `28f2e1f` 基线内容一致。

## 2026-09-14 S0 复审整改与 S1-01 实现

依据：用户要求修复 S0、删除多余总结，随后要求开始 S1，并确认暂无真实工程样本和规则原件。本轮修复 S0 工具和证据口径，推进仅依赖 Day 0 的 S1-01；保持原阶段出口与 A 系列验收要求。

`scripts/check_s0_exit.py` 改为验证实际运行声明、环境输入版本、文件哈希、数据与规则的核验记录、18 组柜台能力、原 A25 编号及缺口关联。模板、合成公告、空供应商、只有合约名的样本和未启动采购不能通过；允许的缺口与已核验状态分别输出。工程样本核验记录绑定六类数据文件及规则登记哈希，跨日手工账按原规格独立重算。

`scripts/init_env.py` 生成本地离线环境报告，检查依赖版本、真实临时 SQLite 文件上的连接参数和外键约束，并登记用户提供的 3 份 SDK 归档及成员哈希。报告明确区分库存记录、可导入状态与未执行的柜台联调、耐久性测试；当前 CTP 候选仍未安装。

数据覆盖、柜台能力、规则来源、A25 适用性与六项缺口分别维护在 `config/` 对应登记文件。移除未经证实的供应商、日期、版本和规则默认值；计费示例移至 `config/rule_sources/examples/`，标记为合成示例。M4 保持“多品种研究交付里程碑”的原含义。09 同步实际出口状态和材料保存约定，README 与 00 提供入口。

已删除重复的 `PROJECT_STATUS.md`、`PROJECT_TREE.txt`、`S0_开发完成总结.md`、`docs/S0_COMPLETION_REPORT.md`、`docs/S0_DONE.md`、`docs/S0_QUICK_START.md`、`docs/S0_SUMMARY.md`；原稿保存在被 Git 忽略的 `runs/review_backups/20260914_s0_summaries_e54064cb/`。项目进度、核验状态和变更依据分别在 README、09、本文维护。

S1-01 实现 `core/constants.py`、`objects.py`、`event.py`、`clock.py`、`ports.py`：不可变数据和命令、显式时间及精度、带作用域的订单/成交标识、未知能力、查询批次、七类端口及基础定时/事件排序。模拟排序策略必须显式传入，录制回放保留汇入序号；未选择实盘事件循环、IPC 或 CTP 封装。实现边界及下一项 S1-02 见 [06 S1-01 记录](06_开发计划.md#s1-01-implementation)，接口细化见 04 §4.8。

验证：`scripts/check_ci.py` 通过 8 项架构测试、274 项单元测试、smoke 与文档校验；单元测试含 72 项核心契约测试和 25 项 S0 工具测试。修改的 Python 文件通过 Ruff，6 个 core 源文件通过 `mypy --check-untyped-defs`。原 4 份规格夹具及冻结规划保持原内容。

当前 S0 出口检查为 3 项 verified、3 项 gap、5 项 pending，无 invalid，返回非零状态符合实际材料缺失情况；不是 S0 全部出口通过。S1-01 的单元与架构交付完成，未执行真实数据接入、A 系列交易验收或柜台联调。

## 2026-09-14 免费数据接入评审整改

依据：用户要求评审免费数据源及样本回测改动，随后选择整改，并明确将执行期间新增的规则库和计费模块一并纳入。原实现虽通过既有 CI，但未覆盖账务、时间、不可变发布及历史规则等关键反例；本次修复不改变 S0/S1/S2/S3 的正式出口。

研究原型从按首根 Bar 计算浮盈改为逐次跟踪开仓成本、已实现盈亏、费用和剩余仓位；通过受可见性约束的端口及虚拟时钟，在信号之后的开盘观察成交。同时间先开盘后信号的保守次序、费用与零滑点等假设写入输出，缺数据或执行价格明确失败。原脚本产生的 −2.62% / 4.42% 等收益率不作为有效验证结果，正式交易内核仍按 S2/S3 开发。

Schema 不再伪造交易日、Bar 起点、结算发布时间或缺失数量；整数数量、完整字段、时区和区间必须通过校验。序列化完整保留来源、接收/导入时间、序号、版本及质量标记。合约解析仅使用显式历史目录和挂牌范围；日历仅查询带来源的实际 Sessions，未知、冲突或无覆盖时不生成交易权限。执行参考必须精确匹配请求，不以日线开盘价回答其他时点请求，也不借用整日成交量。

Parquet 发布改为内容哈希版本及不可变清单，`current.json` 为唯一提交点；数据与清单刷新、校验后提交，失败不会向读者暴露未提交版本。回放固定一个快照。旧两份无版本行情文件和旧 `manifest.json` 保持原内容，避免把有时间错误的改写稿当作已修复数据。原代码及测试备份在本地 `runs/review_backups/20260914_free_data_before_fix/`，规则草稿备份在 `20260914_rule_drafts/`。

同期规则模块已移除假定从 2020 年生效的 rb/FG 默认费率及主连回退；规则存入 SQLite，文件库显式迁移，按作用域、生效时间和获知时间查询。版本不可覆盖、重复登记幂等，替代须显式关联。Domain 计费尊重金额单位及舍入方式，支持按手和按金额合并后舍入；保证金默认保留精度。

通过 HTTPS 重新取得新浪 rb2410 日线 241 条、小时线 1023 条，原始响应归档及哈希已登记到 `config/data_coverage.yaml`。两者均未返回 turnover；日线虽返回结算价字段，发布时间和最终状态仍待核验。原始接入成功不等于规范发布成功，GAP-S0-02 保持未关闭。环境报告已重新生成并覆盖新增 AkShare 依赖；CTP 登录、查询及回报仍未执行。

测试改为使用明确标注的合成目录、日历、来源数据和独立资金预期，取消“本地有数据才断言”的空通过逻辑。新增反例覆盖入场前收益、部分平仓/反向持仓、未来数据、错误时段、发布失败、旧快照、元数据往返、规则替代及非分币舍入。统一 CI 增加 Ruff 语法/未定义名称检查，详细实现边界见 [06 S1 整改记录](06_开发计划.md#s1-data-remediation)。

本轮验证：8 项架构测试、377 项单元测试、smoke、文档校验及新增静态检查全部通过；项目源码和本轮修改的脚本/测试通过完整 Ruff 检查，25 个项目源码文件通过 mypy 检查（第三方缺失的类型存根暂忽略）。另验证发布返回路径固定到本次提交的快照，不受紧随其后的发布影响。原始规划、4 份规格夹具和旧行情样本保持原内容。S0 出口仍为 3 项 verified、3 项 gap、5 项 pending，没有 invalid；资料未完成的状态未因原型测试通过而关闭。

## 2026-09-14 S1-08 交易日志与游标

依据用户“完成剩余工作、每项提交 Git”的指示，先将上一轮数据/规则整改提交为 `7c9fc4d`，再独立实现 S1-08。Journal 的事实去重、状态和预占投影、控制记录及游标采用原子提交；提供幂等事务、受保护的控制代次、准确快照与回放，以及可由 core 端口读取的一致性检查点。类型化序列化不使用 pickle，不接受原始终端数据或凭证字段。

验证覆盖 18 项 Journal 专项测试及 9 项端口测试；包含实际 SQLite 满页失败、提交前子进程退出、重启读回、另一连接提交期间的检查点一致性。修改源码通过 Ruff 和 mypy；完整 CI 由本项提交钩子再次运行。S1-08 的工程实现完成，实际柜台恢复与断电证据继续按对应阶段采集。

## 2026-09-15 S1-09 日志、脱敏和指标

S1-08 已提交为 `d2064eb`，其暂存快照通过 8 项架构测试、395 项单元测试及其余 CI 检查。按用户要求继续独立完成 S1-09。

新增 JSON 日志出口、作用域关联上下文、递归脱敏及日志写入失败状态；处理格式参数、已注册秘密值、异常消息、原始终端载荷、账户标识及 IP/MAC。异常不保存源码行或局部变量。下载和研究入口已接入，并输出线程安全的计数器、Gauge 和单调时钟耗时摘要；标签拒绝原始账号和订单标识，避免把敏感标识写入指标。

23 项专项测试、合计 57 项相关回归通过；覆盖嵌套、中文字段、IPv6 缩写、格式化账户标识、日志故障、线程上下文及并发计数。修改代码通过 Ruff 与 mypy。本项工程实现完成，正式运行的监控门禁和柜台认证仍按 S5 验收。

## 2026-09-15 S1-10 数据质量与缺口工具

S1-09 已提交为 `67424fb`，暂存快照通过 8 项架构测试、418 项单元测试及其余 CI。继续按独立工作项补齐 `validate_data.py`、`gaps.py`、校验实现和报告工具；增加规则库只读连接/一致快照，查询不会创建缺失库或迁移资料。

校验区分原始观察、研究模式和精确模式，检查数据/输入哈希、Schema、合约与时间、价格/成交量、结算、规则和执行价格。缺口依实际 Sessions 求差，正常休市不误报，未知阶段不当作休市；无成交和断线须有证据。失败结果生成隔离清单并返回非零状态，原始与规范文件不被改写。

19 项专项测试与含规则库在内的 30 项回归通过，修改代码通过 Ruff 和 mypy。实际运行确认两份新浪归档均缺 turnover，质量报告及隔离清单保存在 `runs/s1-10/`，报告哈希已登记到数据覆盖清单；6 项准备缺口保持未关闭。此项完成的是工程工具，不宣称 S0/S1 的外部资料与正式验收已完成。

## 2026-09-17 S1 数据源完善与真实工程样本发布

依据用户“审查 S1 是否完成并按建议执行”的指示，完成数据源适配完善、真实合约目录构建及首个工程样本规范发布：

1. 适配并完善 `TushareFuturesDataSource`，支持 `captures` 原始响应留痕、`amount` 万元换算为元 `turnover`、结算价转换与日期升序排序；增加脱机单元测试，不依赖外部实时网络；
2. 编写 `scripts/build_contract_catalog.py` 从官方基础数据接口生成标准历史合约目录 `config/contract_catalog_2024v1.json`（386 条合约记录），覆盖 SHFE/CZCE/DCE，通过 `ContractResolver.from_file` 验证 A29 郑商所短代码解析；
3. 下载首个工程样本 `SHFE.rb2410` 从上市到摘牌全部 241 个交易日的日线数据，原始响应哈希留痕，补齐真实 turnover 与结算价；
4. 编写 `scripts/build_sample_metadata.py`，生成配套交易日历 `config/calendar_2024v1.json` 与时间/结算证据 `config/timing_rb2410_1d.json`，原子发布至不可变 Parquet 并生成 723 条执行价格参考；
5. 运行 `validate_data.py`（research 模式）与 `gaps.py`，数据质量报告通过（0 error，0 隔离，0 缺口），在 `config/data_coverage.yaml` 中完成登记；
6. 重新生成环境报告 `runs/s0/environment.json`，修复输入哈希不一致；全量 448 项架构与单元测试通过，全套 CI 全绿。

## 2026-09-17 S2-01 / S2-02 订单状态机与四字段持仓内核实现

依据用户“提交并推进到 S2”的指示，正式启动 S2 共享交易领域内核的工程实现：

1. 实现 S2-01 `domain/orders.py`：
   - 订单领域实体 `Order` 与严格单向状态机，强制防倒退规则（终态后不退回中间活动状态，但保持迟到成交入账能力）；
   - 独立发送状态 `SendState`（NOT_SENT / SENT_UNKNOWN / CONFIRMED_REMOTE）与独立撤单状态 `cancel_pending`；
   - 严格成交去重器 `TradeDeduplicator`，基于 `TradeKey`（账号、交易所、交易日、成交编号及扩展作用域）确保相同事实不重复记账；
   - 待关联真实成交暂存机制 `UnlinkedTrade`，确保成交先于报单回报到达时不丢弃真实成交，报单到达后自动补关联。
2. 实现 S2-02 `domain/positions.py`：
   - 持仓四字段明细实体 `PositionDetail`，维护 `pos_yd`, `pos_td`, `frozen_yd`, `frozen_td` 核心不变量（`0 <= frozen <= pos`）；
   - 逐单预占 `PositionReservation` 与生命周期管理；
   - 终态撤销回报到达时多撤预占保留机制（`terminal_before_fills`）：若柜台累计成交大于已入账成交，保留差额冻结，等待迟到成交冲抵；
   - 跨日交易日推进 `advance_trading_day()`，完成今仓并入昨仓与隔夜平仓挂单冻结结转。
3. 验证与测试：
   - 新增 `tests/unit/test_domain_orders.py` 与 `test_domain_positions.py` 共 13 项专项测试；
   - 严格匹配并验证 `tests/fixtures/order_event_examples.json` 中全部 4 个规格场景；
   - 全量 461 项测试全过，代码通过完整 Ruff 检查。

## 2026-09-17 S2 共享交易领域内核完整闭环实现 (S2-03 ~ S2-11)

依据用户“继续完成 S2”的指示，完成 S2 共享交易领域内核全部关键模块开发与端到端闭环对账：

1. **S2-03 事件账本与资金模型 (`domain/ledger.py`)**：
   - 独立实现双盈亏视图：`mtm_close_pnl`（逐日盯市平仓盈亏，今仓对开仓价、昨仓对上一结算价）与 `trade_close_pnl`（逐笔交易平仓盈亏，FIFO 开仓批次对冲）；
   - 官方结算价日终结转 (`settle_day`)：将未平持仓盯市盈亏划入结存余额并更新次日计价基准；
   - **严格对齐规划 §4.4 与 `tests/fixtures/ledger_examples.json` 独立手工账**：首日结算 +100，次日盯市平仓 -20，逐笔平仓 +80，权益合计净增 80，逐笔盈亏绝不重复计入余额；
   - 实现资金三字段模型（`broker_available` 柜台可用、`available_for_new_trades` 保守开仓额度、`margin_coverage_equity` 保证金覆盖权益）；
2. **S2-04 智能平仓路由 (`domain/smart_router.py`)**：
   - 纯函数式生成子单计划（无副作用），支持 `ClosePriority`（平昨优先 / 平今优先）；
   - 映射上期所/能源中心（SHFE/INE）强平今平昨分离规则，自动分配子订单并继承 `parent_order_id`；
3. **S2-05 & S2-06 & S2-10 交易所硬约束、原子风控与控制代次 (`domain/limits.py`, `domain/risk.py`)**：
   - 交易所硬约束 `ExchangeLimits`：日内开仓限额、一般持仓限额、撤单次数限额；
   - 原子事前风控 `RiskManager`：自成交交叉挂单防范（FR-RISK-04）、熔断状态机（NORMAL/REDUCE_ONLY/HALTED，且合法平仓减仓单绝不被开仓风控阻断）、开仓资金可用性核验；
   - 控制代次校验（FR-RISK-07, A23）：过期代次交易命令被拒，但旧代次已发生的真实成交客观入账；
4. **S2-08 & S2-09 & S2-11 恢复对账、生命周期门禁与内存 Journal (`domain/recovery.py`, `domain/lifecycle.py`, `infrastructure/memory_journal.py`)**：
   - `RecoveryCoordinator`（A04）：检测外部未知委托与持仓差异，存在阻断性差异时禁止进入 READY；
   - `DailyLifecycleManager`（A26）：登录成功不自动恢复交易，半结算保护（`SEMI_SETTLED_HOLD`，F10）严格禁止新增风险；
   - `MemoryJournal`（FR-REC-01）：纯内存版 `JournalPort`，支持不可变事务原子提交、成交防重、游标冲突阻断与历史回放；
5. **测试与 CI**：
   - 新增 15 项领域集成单元测试（累计 S2 新增 28 项测试）；
   - 全套 476 项架构与单元测试通过，全套 CI 全绿通过。

## 2026-09-17 S2 领域内核深度复审整改 (对齐 cpa/deepseek-v4-pro 审查项)

依据用户指定的 `cpa/deepseek-v4-pro` 独立架构与量化交易审查意见，针对识别出的核心缺陷进行专项修复与闭环验证：

1. **修复 D9（智能拆单路径不可达，阻塞 A01）**：
   - 在 `core/constants.py` 中为 `Offset` 补充 `CLOSE` 枚举值，代表策略发出的通用平仓意图；
   - 在 `domain/smart_router.py` 中激活智能平仓拆单逻辑，根据品种持仓的今昨分布与 `ClosePriority`（平昨优先 / 平今优先）拆解为明确的 `CLOSE_YESTERDAY` / `CLOSE_TODAY` 子单，并继承父单 `parent_order_id`；新增平昨优先与平今优先专项测试；
2. **修复 D12（生命周期跨日未清除半结算警告，阻塞 A26）**：
   - 在 `domain/lifecycle.py` 的 `advance_trading_day()` 中显式重置 `semi_settled_warning = False`，避免上日半结算保护状态意外延伸至新交易日；增加跨日重置与恢复交易测试；
3. **修复 D3 & D4（冻结跨桶消费不完整）**：
   - 在 `domain/positions.py` 中统一 `PositionDetail.apply_fill()` 与 `PositionManager.apply_trade()` 的冻结桶扣减逻辑：优先扣减目标桶，若因差额保留跨桶分布导致目标桶不足，自动从另一桶补扣，杜绝幽灵冻结；
4. **修复 D1 & D2（订单成交量防御性上界）**：
   - 在 `domain/orders.py` 中为 `apply_order_update()` 和 `apply_trade()` 增加成交量超过委托量的防御性上限限制与对账标记 (`reconciliation_required`)；
5. **修复 D6（账本开仓批次封装与空批次恢复兜底）**：
   - 在 `domain/ledger.py` 中为 `InstrumentLedger` 暴露只读批次属性 `open_long_lots` / `open_short_lots`；在 `settle_day()` 中增加极端回放恢复下无批次持仓以基准结算价兜底计算的防御分支；
6. **全量验证**：
   - 全量 478 项测试全绿通过，CI 检查全部通过。




## 2026-09-18 S2 完成情况审查与逐项修复

依据用户指示"审查 S2 完成情况"并"按照建议逐项修复"。审查由三组独立代理按 FR / A 条款逐项核对，并以脚本复现账本缺陷；结论为 S2 不能签收。修复覆盖全部审查项：

1. **S2-03 账本重写 (`domain/ledger.py`)**：计价基准按开仓批次维护 (`PositionLot.basis`)，结算只对剩余批次计提 (结算价 − 基准) 并把基准更新为结算价，修复多日持仓重复结算 (复现值 450 → 正确 250)；平今只匹配当日批次、平昨只匹配已结算批次、统一平仓先昨后今，修复平今取最老批次 (复现值 +280 → 正确 −20)；新增结算版本 (`SettlementRecord`)：同版本重复为空操作，修订以带版本的差额更正事件处理，旧版本拒绝，结算价缺失抛 `SettlementPendingError` 且不推进交易日；账本改为不可变事件 (`LedgerEntry`) 推导，金额只在入账时按最小货币单位舍入 (FR-LED-04)；外部现金流单独记录，`strategy_pnl` 剔除；新增逐单资金预占 (`reserve_funds` / `release_funds`)，`frozen_margin` / `frozen_fee` 由预占汇总；浮盈使用权限改为显式 `FundsPolicy` 版本。
2. **S2-02 持仓重写 (`domain/positions.py`)**：冻结只按逐单预占消耗，无预占成交不得侵占其他订单冻结 (`PositionAccountingError`)；新增 `verify_frozen_invariant` 校验聚合冻结等于预占汇总；统一平仓 `CLOSE` 可预占与成交；跨日转换按交易日幂等，预占的平今在跨日后转为平昨；上一交易日迟到成交按其原交易日入账 (FR-CAL-06)。
3. **S2-01 / S2-07 订单重写 (`domain/orders.py`, `core/ports.py`)**：删除按品种、方向猜配成交的逻辑，成交只按 `Trade.order_identity` 中可唯一归属的远端标识关联，无法归属进入待关联队列 (`resolve_unlinked_trade` 供确认后一次性入账)；无法归属的订单回报记为外部占位 (`ExternalOrderRecord`) 而非抛异常；`send_state` 只能向更强证据推进，迟到的本地 NOT_SENT 不回退已远端确认的订单；对账标记只能由明确处置清除；去重键包含适配器 `extra_scope`；`OrderStatus` 补 `EXPIRED` / `UNKNOWN`；撤单拒绝保留原因；报单前登记原会话三元组 (`bind_session_identity`)；新增 `FeedbackNormalizerPort` 归一化契约。
4. **S2-04 / S2-05 / S2-06 / S2-10 (`domain/smart_router.py`, `domain/limits.py`, `domain/risk.py`)**：路由引入柜台能力表 (`CloseCapabilityTable`)，不支持的平今 / 平昨明确拒绝 (`UnsupportedOffsetError`)，统一平仓交易所整单透传并输出费用归属；品种级意图解析 (`ProductIntent`)；删除无来源的 500 / 490 生产默认值，限额改为带来源 (交易所 / 券商 / 内部三类)、生效日期与证据引用的版本化规则；新增交割月资格与涨跌停价带检查；风控计数按 (交易日, 合约) 键并可快照 / 恢复；熔断状态机只允许前进，重置需 cause_cleared 与 account_consistent 同时成立；开仓冷却不阻断平仓 / 撤单；控制代次含 `controller_id`，撤单、改单、恢复命令统一校验；`check_and_reserve` 原子完成检查、持仓冻结与资金预占。
5. **S2-08 / S2-09 (`domain/recovery.py`, `domain/lifecycle.py`)**：恢复协议 `DISCONNECTED → RECOVERING → RECONCILING → READY`，恢复控制记录与快照游标，重放事件；查询按批次保存范围、交易日、完成标志与合并水位，未完成 / 限流 / 跨交易日混合的查询不作快照；差异双向识别 (外部委托、本地活动单查无、远端独有持仓、冻结与套保标志)；READY 不解除既有熔断；恢复超时与重复断线告警。生命周期新增 `SessionClosed` / `SettlementReady` / `TradingDayAdvanced` 事件，按合约收盘，账户级半结算快照保护 (待核对快照不覆盖最后一致快照)，日终任务按 (账户, 交易日, 类型, 版本) 幂等，`can_reduce_risk` 与新增风险门禁分离。
6. **S2-11 (`infrastructure/memory_journal.py`)**：与 SQLite 日志语义对齐 (重复事务内容校验、回放物化、按序号重建快照、全部守卫)，新增 `export_events`；新增双适配器共享端口契约测试。
7. **S2-12 可控网关夹具 (`tests/fixtures/controllable_gateway.py`)**：生成规范回报与查询结果，支持重复、乱序、迟到、跨会话、限流注入；新增 `test_domain_kernel_gateway_injection.py` 驱动订单 + 持仓 + 账本组合执行序列，逐事件断言持仓、冻结与入账次数。
8. **验证**：550 项测试通过 (较修复前新增 72 项)，`ruff` 全绿，`scripts/check_ci.py` 通过。`tests/fixtures/*.json` 的 `execution_status` 仍为 `not_executed`：夹具规格已由测试执行，但柜台联调与真实规则核验未做，正式出口仍以 09 为准。

## 2026-09-18 S3 单品种简化 Bar 回测与快速研究通道开发完成

依据规划 §9.1 与 06 开发计划，完成 Stage S3（单品种简化 Bar 回测与快速研究通道）全部核心组件开发与端到端闭环验收：

1. **S3-01 & S3-03 模拟撮合网关 (`gateway/simulated_gateway.py`)**：
   - 实现 `ExecutionPort` 协议，完全满足架构隔离，严禁直接依赖 domain 或 engine；
   - 实现确定性 Bar 级撮合：开盘撮合 (`NEXT_BAR_OPEN`) 与盘中限价触价撮合 (`low <= limit` / `high >= limit`)；
   - 涨跌停流动性情景 (`LimitLiquidityScenario`)：支持 `DIRECTION_CONSERVATIVE` (方向保守，涨停买单不成交卖单成交) 与 `TOUCH_LIMIT_NO_FILL` (触板无成交压力情景)；
   - 严格落实参与率共享预算 (`participation_rate`) 与零成交量不成交 (A11, A20, FR-MATCH-01~04)；
   - 生成规范的 `CanonicalEvent[OrderUpdate]` 与 `CanonicalEvent[Trade]` 回报流。
2. **S3-04 策略端口与 CTA 示例策略 (`strategy/base.py`, `strategy/examples/trend_following.py`)**：
   - 在 `core/ports.py` 中定义纯粹的 `StrategyPort` 与 `StrategyContextPort` 协议，使策略层仅依赖 Core 协议；
   - 交付 `StrategyBase` 抽象基类；
   - 实现双均线趋势跟踪策略 `DualMovingAverageStrategy`，严格基于已结束 Bar 指标计算，无任何未来信息渗透。
3. **S3-02 交易引擎与回测调度 (`engine/base_engine.py`, `engine/backtest_engine.py`)**：
   - 严格遵循六边形架构依赖反转原则（Engine 仅依赖 Core 与 Domain，通过注入端口接入 Gateway 和 Strategy）；
   - 深度集成 S2 交易领域内核：订单管理器、四字段持仓预占、双盈亏事件账本（逐日盯市平仓盈亏与逐笔平仓盈亏）、智能拆单路由与每日结算结转 (`settle_day`)；
   - 产出结构化 `BacktestResult` 与逐 Bar 资产快照 (`EquitySnapshot`)。
4. **S3-05 & S3-06 & S3-07 绩效分析、实验快照与报告生成 (`analysis/`, `scripts/run_backtest.py`, `scripts/report.py`)**：
   - `analysis/performance.py` 计算累计/年化收益率、年化波动率、夏普比率、最大回撤、卡玛比率、胜率与手续费占比；
   - `analysis/validation.py` 实现严格时序样本划分 (`train_test_split`, `walk_forward_slices`)；
   - `scripts/run_backtest.py` 驱动真实 Parquet 工程样本，生成不可变 `run_manifest.json` 实验快照；
   - `scripts/report.py` 生成规范 Markdown/文本绩效报告。
5. **S3-08 & S3-09 & S3-10 向量化通道、行情回放与 A25 规则边界 (`research/`, `scripts/scan_params.py`, `scripts/replay_market_data.py`)**：
   - `research/cost_assumptions.py` 与 `vector_backtest.py` 提供轻量级参数网格快速扫描；
   - `scripts/replay_market_data.py` 实现全链路确定性行情回放；
   - `tests/unit/test_s3_acceptance_a25_a28.py` 严格验收 A28 回放幂等性、A11 零量因果律与 A20 触板压力情景。
6. **S3-11 冒烟脚本与全量验证 (`scripts/smoke.py`, tests)**：
   - 扩展 `scripts/smoke.py` 支持回测链路端到端自动化冒烟检查；
   - 新增 14 项完整端到端测试，全量 564 项架构与单元测试全绿通过，CI 检查全绿通过。

## 2026-09-18 S4 多品种、移仓与节前处理开发完成

依据规划 §4.3、§4.5.4 与 06 开发计划，完成 Stage S4（多品种、移仓与节前处理）全部核心组件开发与 A10 验收测试：

1. **S4-01 当时可知主力映射 (`data/dominant_contract.py`)**：
   - 交付 `DominantContractResolver` 与 `build_dominant_mappings`，基于决策时点可见的持仓量与成交量推导主力合约；
   - 引入连续确认天数机制（`confirm_days=2`，防假突破频繁换月），生成带不可变版本与有效区间的映射；
   - 严格因果时序：T 日日盘结束决策，T+1 日首个交易时段生效，不回溯改写。
2. **S4-02 连续序列与同合约收益率视图 (`data/continuous.py`)**：
   - 交付 `ContinuousSeriesBuilder`，支持原始拼接 (`RAW`)、价差平移 (`DIFF`) 与比例复权 (`RATIO`)；
   - **严格消灭虚假动量 (A10)**：切换日收益率基于同合约自身的日内/跨日涨跌计算，绝不使用 `(新合约今日收盘 / 旧合约昨日收盘 - 1)` 计算虚假收益；
   - 撮合、持仓与账本估值始终使用真实合约原始价格。
3. **S4-03 移仓两腿状态机 (`domain/rollover.py`)**：
   - 交付 `RollManager` 与 `RollTask`，两腿状态机严格按 `PLANNED -> LEG_1 -> LEG_2 -> COMPLETED` 递进；
   - 支持 `CLOSE_FIRST` (先平后开，资金安全) 与 `OPEN_FIRST` (先开后平，敞口连续) 两种腿序；
   - 细化分批移仓与部分成交推进，异常阻断进入 `PAUSED` 并保留已成交事实。
4. **S4-04 节前长假风控钩子 (`domain/risk.py`)**：
   - 在 `RiskManager` 中集成 `HolidayRiskHook`，在节假日前指定窗口期内阻断一切新开仓命令（`prevent_new_open`）；
   - 平仓与合法减仓操作不予阻断，复市后自动恢复正常交易。
5. **S4-05 & S4-06 多品种归因与移仓报告 (`analysis/performance.py`, `analysis/visualizer.py`)**：
   - `PerformanceMetrics` 扩展分品种成交贡献与移仓展期价差归因明细；
   - 移仓成本真实反映两腿成交差与实际手续费，价差本身不计入账本现金盈亏。
6. **验证与全量 CI**：
   - 编写 `test_dominant_contract.py`、`test_continuous_series.py`、`test_rollover.py`、`test_holiday_risk_hook.py` 与 `test_s4_acceptance_a10.py`；
   - 全套 570 项架构与单元测试全绿通过，CI 门限检查全绿通过。

## 2026-09-18 S3 独立架构与量化交易审查整改 (对齐 cpa/deepseek-v4-pro 审查项)

依据 `cpa/deepseek-v4-pro` 独立评审专家报告，针对识别出的核心缺陷进行专项修复与闭环验证：

1. **修复 P0-01（涨跌停边界在回测引擎中从未传递到撮合器，阻塞 A20）**：
   - 在 `engine/backtest_engine.py` 的 `run()` 中增加 `price_limits` 参数支持，并在调用 `match_bar` 时显式传递 `upper_limit` 和 `lower_limit`；
   - 在 `tests/unit/test_s3_acceptance_a25_a28.py` 中更新 `test_a20_limit_scenario_touch_no_fill`，构造真实涨停 Bar 并传递涨跌停价格，严格验证在 `TOUCH_LIMIT_NO_FILL` 压力情景下触板严禁成交、顺延至未触板 Bar 开盘成交。
2. **修复 P0-02（绩效指标中胜率与盈亏比计算错误）**：
   - 彻底废除原先按总盈亏简单二值判断的假逻辑；
   - 在 `analysis/performance.py` 中实现 `_compute_paired_trade_pnls`，基于实际成交序列采用 FIFO 开平仓配对规则，逐对计算扣除手续费后的真实净盈亏；
   - 真实统计平仓盈利笔数 (`winning_trades`)、亏损笔数 (`losing_trades`)、实际胜率 (`win_rate`) 与盈亏比 (`profit_loss_ratio`)；在 `test_performance_analysis.py` 中增加专项用例。
3. **修复 P0-03（手续费硬编码，未接入规则库）**：
   - 在 `engine/base_engine.py` 与 `backtest_engine.py` 中引入 `RuleStorePort` 端口注入；
   - 在处理成交回报时，优先调用 `rule_store.commission_rule` 按合约、业务时刻及开平今昨 offset 动态查询费率并精确计算；未注入时平滑回退到默认每手固定费率。
4. **修复 P2-02（CTA 双均线策略死叉开空与金叉平空方向错误）**：
   - 在 `strategy/examples/trend_following.py` 中修正：死叉开空使用 `self.context.sell(..., offset=Offset.OPEN)`，金叉平空使用 `self.context.buy(..., offset=Offset.CLOSE)`。
5. **修复 P1-01 & P2-03（完善 run_manifest 元数据与报告输出）**：
   - 在 `scripts/run_backtest.py` 中记录真实 Git commit hash、Python 版本、数据快照 ID 以及完备绩效指标字典；
   - 在 `scripts/report.py` 中补充夏普比率、最大回撤、真实胜率与可追溯代码版本展示。
6. **全量验证**：
   - 570 项测试全绿通过，`scripts/check_ci.py` 全绿通过。

## 2026-09-19 S3 评审与修复

1. **评审**：`docs/reviews/S3_回测引擎实现评审意见.md` 判定 2026-09-18 的 S3 交付为 S3a 内部里程碑，列出 5 项阻塞（向量化前视偏差、网关回报未入内核与预占不释放、A20 开盘候选被收盘触板否定、run_manifest 缺哈希且覆盖写入、S3-01/09/10 未实做）与 9 项主要缺陷。
2. **修复**（详见 06 S3 实现记录表）：
   - `gateway/simulated_gateway.py` 重写：两相撮合、价格域与步长约束、预算不凑整、GFD 过期、到达延迟与时段权限（`SessionGatePort`）、出站事件只投递一次；
   - `engine/base_engine.py` / `backtest_engine.py` 重写：回报经 `EventQueue` 入内核并释放预占、`RiskManager.check_order` 统一事前风控、按合约经济参数、执行时点策略（`NEXT_SESSION_OPEN` / `NEXT_DAY_SESSION_OPEN`）与错过执行处置、定时器、Journal 落盘、多品种时间线与全持仓结算、`canonical_hashes()`；
   - `research/vector_backtest.py` 改为下一 Bar 开盘成交；新增 `research/backtest_assembly.py` 统一装配与 run_manifest；
   - `analysis/` 指标口径改为账本事实与交易日频率，新增敏感性矩阵与权益曲线输出；
   - 新增 `data/session_gate.py`、`tests/fixtures/sessions/A25-04/`、`tests/fixtures/backtest_expectations.json`；
   - `core/ports.py` 新增 `SessionGatePort`、`SimulatedExecutionPort`、`StrategyPort.on_timer`；`core/constants.py` 新增 `IntrabarTouchRule`、`AuctionFillPolicy`、`MissedExecutionPolicy`。
3. **验证**：609 项测试通过；`ruff` 对本次改动文件全绿（`scripts/report.py` 等三个入口脚本按 pyproject 豁免 E501）；`scripts/check_ci.py` 通过。`ruff --fix` 顺带移除了 `data/continuous.py`、`data/dominant_contract.py`、`domain/rollover.py` 的未使用导入。
4. **遗留**：A25-02/03/05 夹具与历史原文、A25-07 ExecutionReference 降级路径、A05 特殊日历与 A16 质量标记接入回测、`NEXT_DAY_FIXED_TIME` 执行策略（需盘中执行数据）。S4 的多品种归因与移仓在新引擎上需回归。


## 2026-09-19 S4 多品种组合、移仓执行与样本外报告收口

1. **S4-05 测试品种组合接入**：
   - 新增 `data/product_registry.py`（rb/fg/MA/c/m/i/AP/jd/au/cu 的乘数、最小变动、保证金、手续费、日盘竞价方式与研究假设标记）；
   - 新增 `data/session_templates.py`（夜盘报单窗/竞价/连续时段、日盘三段时间与小休、长假夜盘规则）与
     `data/calendar.py::project_product_calendar`（把品种模板投影到本次交易的合约集合）；
   - 新增 `scripts/build_s4_sessions.py` → `config/sessions_s4_2024v1.json`（schema_version 2，10 个品种、561 个交易日）；
     新增 `scripts/build_s4_contract_catalog.py` → `config/contract_catalog_s4_2024v1.json`（64 个实际合约，由可观察序列推导）；
   - 新增 `scripts/fetch_s4_test_products.py`，经新浪免费源归档 10 个品种的实际合约日线；缺 turnover 按
     `QualityFlag.TURNOVER_UNAVAILABLE` 标记，禁止进入精确核算（FR-DATA-08/A16）；
   - `config/data_coverage.yaml` 登记 S4 数据集与各测试品种接入状态，`c` 一并登记。
2. **S4-06 多品种样本外报告与移仓归因**：
   - 新增 `research/multi_product.py`（连续序列信号 + 实际主力成交 + 真实两腿移仓 + 组合权益前向填充聚合 +
     样本内外切分 + 移仓价差/手续费/两腿暴露归因）；
   - 新增 `scripts/run_multi_product.py` → `runs/s4/multi_product/report.md`、`run_manifest.json`；
   - 实测 8 品种 2024-06-03 ~ 2026-09-11：组合 3,200,000 → 3,586,508，拟合窗 +0.54% / 保留窗 +11.09%，
     38 次移仓全部两腿成交，未成交与错过执行均为 0。
3. **新引擎缺陷修复（由真实多品种日线回测暴露）**：
   - 日线级 GFD 过期导致收盘后的委托从未获得成交机会就被过期；改由版本化时段门决定可报单时刻；
   - 引擎改写平今/平昨并拆子单后父单号丢失，移仓状态机停在第一腿；新增 `RollManager.on_fill()` 按合约推进两腿；
   - 跨日结转改写预占桶但成交仍按原 offset 扣桶，持仓与账本不一致；`PositionManager.effective_offset()` 以预占冻结桶为准；
   - `Order.created_at` 使用墙钟，回测清单委托时间不可复现；改为取意图创建时刻。
4. **补齐 A25 时段夹具**：`tests/fixtures/sessions/A25-02|A25-03|A25-05/`（日历 + 期望）与
   `tests/unit/test_s3_a25_fixtures.py`，覆盖夜盘归属、跨日/跨节时段与只撤不报窗口，关闭本文件 2026-09-19 S3 条目“遗留：A25-02/03/05 夹具”一项。
5. **验证**：全量 615 项测试通过（新增 `tests/unit/test_s4_multi_product.py` 7 项）；`scripts/check_ci.py` 全绿。
6. **未关闭缺口**：GAP-S0-03（研究数据集未采购）、经济参数与官方日历待核验、结算价使用当日收盘价假设；
   全部登记在 `config/data_coverage.yaml` 与运行清单中。
